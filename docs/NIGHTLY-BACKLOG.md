# Nightly backlog

Carried between autonomous nightly maintenance runs so they compound instead of
rediscovering the same ground. Read this first; mark resolved items Closed; do
not re-propose anything already Open; append what you check and rule out.

## Open

- [ ] 2026-09-01 — **`globToRegex` translates `**` to a bare `.*`, so a mid-pattern `**/` matches inside a path segment and widens every allow-list built on it** — `lib/agent-runtime/paths.mjs:5`. `wiki/**/decisions.md` compiles to `/^wiki\/.*decisions\.md$/`; the `.*` is not anchored to a separator, so `wiki/EVILdecisions.md` and `wiki/a/b/XXdecisions.md` both match a rule that was written to mean "a `decisions.md` inside some directory under `wiki/`". Reproduced against `assertWriteAllowed` directly — all three `allowed: true`. This feeds three guards: `allowed_writes`/`forbidden_paths` here, `vault_writes` at `vault-writeback.mjs:30`, and the context include rules in both `context-loader.mjs` files. Direction of failure is asymmetric: in an allow-list it **widens permission**; in `forbidden_paths` it over-denies, which is the safe direction. Distinct from the 2026-08-26 variable-widening item below — this needs no variables at all, it is the translator itself.
  - **Latent, not live, and that is why it is filed rather than patched.** Every `**` in the tracked config today is a *trailing* `prefix/**` (checked all of `config/`: 30 distinct patterns, of which **zero** have `**/` in the middle), and trailing `**` is correct — `wiki/**` compiles to `^wiki\/.*$`, which is what it should mean. The bug needs a `**/` mid-pattern shape that no contract currently uses. So there is no exploitable path today, and a fix would have nothing real to verify against.
  - The fix itself is small — emit `(?:[^/]+\/)*` for a `**` that is followed by `/`, keep `.*` for a trailing `**` — but it changes the semantics of the matcher behind three guards, so it wants review rather than a nightly. Pinned meanwhile by `tests/agents/write-guard.test.mjs` (`GAP: ** translates to a bare .* so it matches inside a path segment`), which asserts the CURRENT behaviour, so whoever fixes this will see that test fail and should invert it deliberately.

- [ ] 2026-09-01 — **`assertReadAllowed` never calls `checkUnsafePath`, so the read guard accepts traversal that the write guard rejects** — `lib/agent-runtime/paths.mjs:76` goes straight to `forbidden_paths` and `allowed_reads`, while `assertWriteAllowed` (line 57) opens with `checkUnsafePath(relPath)`. Reproduced: `../../etc/passwd` is `allowed: false, reason: "unsafe path: dot-segment traversal"` for a write, and `allowed: true` for a read under `allowed_reads: ["**"]`. This is the "a guard exists, and covers half its input space" shape rather than a live hole: `assertReadAllowed` has **no callers in the repo** — it is exported public API declared in `lib/agent-runtime/index.d.ts`, and reads are actually evaluated by `context_policy` in `context-loader.mjs`. Filed because the asymmetry is invisible at the call site: anything that wires it up inherits a guard that looks like the write guard and is not. Pinned in `tests/agents/write-guard.test.mjs`.

- [ ] 2026-09-01 — **The repo-scaffolding templates build YAML by string interpolation, and `upsertRepo` does not validate `repo_name`, so the guard lives at the callers rather than at the data** — all four generators in `lib/repo-runtime/templates.mjs` (`generateCanonicalTemplate`, `generateHomePage`, `generateProgressPage`, `generateRepoCLAUDE`) emit frontmatter as `title: "${repoName} — …"` instead of going through `serializeFrontmatter`. A repo name containing `"` and a newline closes the scalar and adds arbitrary top-level keys: reproduced with `repo" \ninjected_key: pwned\nx: "`, after which `parseFrontmatter(...).data.injected_key === "pwned"` for every one of the four.
  - **Not reachable today**, which is why this is a pin (in `tests/repos/repo-templates.test.mjs`) and not a fix: the four generators have no callers in-tree (only declarations in `lib/repo-runtime/index.d.ts`), and the live MCP and CLI entry points run `repo` through `validateSlug` (`mcp/server.js:1040,1059,1165`; `cli/kb.js:1451`), which rejects both characters.
  - The structural point, and the reason it is worth an entry: `upsertRepo` (`lib/repo-runtime/registry.mjs:85`) writes whatever `repo_name` it is handed, with no `validateSlug`. Every current writer happens to validate upstream. The cheap durable fix is to validate at `upsertRepo` so the invariant holds at the store rather than at each caller; the thorough one is to route the four generators through `serializeFrontmatter`, which is the same "make there be one codec" point as the frontmatter item below.

- [ ] 2026-08-30 — **The PII deny-list is now three hand-maintained copies of the same regex set, and they were already out of sync** — the two `scrub-raw-pii.mjs` fixes shipped tonight both trace to the same structural cause, so the fix for the *class* is a design change rather than a third round of pattern-copying. The three copies: `scripts/hooks/pre-commit`'s `PII_PATTERNS` (bash, `grep -Ei`, ~20 patterns, the authority), `scripts/scrub-raw-pii.mjs`'s `REDACTABLE` + `REPORT_ONLY` (JS `RegExp`, was 6 patterns, now 15), and `tests/pii-pattern-probe.sh`'s `PATTERNS` (bash, 8 patterns, a copy of one *block* of the hook that will silently stop testing the hook the moment the hook is edited). The script's own header carries a "Coupling note" instructing a human to keep them in sync, which is the tell: a comment is the only thing enforcing it, and it did not hold — nine of the hook's patterns had no scrubber counterpart, including bare `\bwife\b`/`\bhusband\b`/`\bspouse\b` and a named person, all of which are ordinary words in captured third-party web pages.
  - Why this is not "just add the patterns" (which is what tonight's commit did, as the stop-gap): the failure mode is silent and asymmetric. The hook is the enforcement point; the scrubber is the *preflight* whose exit 0 means "nothing blocking a commit". Any pattern present in the hook and absent in the scrubber turns into a confident false negative, and there is no signal at all until a night job deadlocks on a file the preflight cleared. The probe has the mirror-image problem: it can only ever assert things about its own copy.
  - The proposal: one machine-readable pattern file — `config/pii-patterns.json` or similar — carrying `{ name, pattern, flags, action: 'redact' | 'report' }` per entry, with the hook reading it (`jq -r '.[].pattern'` into the existing `for pattern in` loop, no other bash change), `scrub-raw-pii.mjs` building `REDACTABLE`/`REPORT_ONLY` from the same file, and the probe iterating it. That is the "make there be one list" shape, the same argument the frontmatter entry below makes for parsers. Acceptance criterion: deleting a pattern from the JSON must make `tests/scrub-raw-pii.test.mjs` fail, and adding one must require no edit to any of the three consumers.
  - Not attempted tonight because it edits the pre-commit hook's control flow, and a bug there fails *open* on the repo's only secret/PII gate. That wants a deliberate review, not a nightly.


- [ ] 2026-08-29 — **The committed `mcp/node_modules` is a partial snapshot that `npm ci` does not reproduce, and the 2026-08-20 entry below asserts the opposite** — that entry closed the vendoring question with "if it is ever un-vendored, `mcp/package-lock.json` is committed and reproduces it exactly." Measured tonight, it does not. `npm ci --ignore-scripts` from a copy of `mcp/package.json` + `mcp/package-lock.json` into a clean dir, then `diff -rq` against the committed tree: **3465 files committed vs 3484 installed**, 19 files present only in the fresh install. Nothing *differs* in content and nothing is present only in the committed tree — the drift is one-directional omission, not modification.
  - The mechanism, and why it will keep getting worse: `.gitignore:5` lists `node_modules/`, so the 3,465 files are tracked only because they were force-added once (`c6034643`, 2026-04-07). Any later `npm install`/`npm ci` in `mcp/` updates the working tree, and git records modifications to already-tracked files but **never picks up newly-added ones** — they are ignored. So every dependency change since 2026-04-07 has silently widened the gap, and every future one will. There is no gate that would notice.
  - The concrete consequence today: `body-parser@2.3.0` and `type-is@2.1.0` both declare `content-type: ^2.0.0`, and `npm ci` satisfies that with nested `body-parser/node_modules/content-type@2.1.0` and `type-is/node_modules/content-type@2.1.0`. Neither nested copy is committed. From a fresh `git clone`, `createRequire(mcp/node_modules/body-parser/index.js)('content-type/package.json').version` returns **1.0.5** — the hoisted copy, a major version below the declared range. Same for `type-is`. Also missing: three `hono@4.13.2` `method-not-allowed` middleware builds (`dist/cjs`, `dist`, `dist/types`) at a version that otherwise matches.
  - **Not live, and that is the only reason this is an Open item rather than a fix.** `mcp/server.js` imports `StdioServerTransport` and nothing else (line 15, instantiated 1316); express / body-parser / type-is are never loaded on the stdio path. Verified end to end: a fresh `git clone` of `main` runs `node mcp/server.js` to a clean exit. The mis-resolution is latent — it becomes real the day anyone adds an HTTP transport, and it will present as a semver-impossible bug that reproduces only from a clone and never after `npm ci`.
  - Two ways out, both the repo owner's call, which is why neither was taken: (a) un-vendor and add an install step, contradicting the deliberate 2026-04-07 decision that a stdio server should launch straight out of a clone; (b) keep vendoring but `git add -f mcp/node_modules` after every dependency change, and add a CI check that `npm ci` into a temp dir diffs clean against the committed tree. (b) is the smaller change and is what makes the invariant checkable instead of assumed. **Hazard for a nightly runner attempting (b):** the 19 files include `mcp/node_modules/fast-uri/.github/workflows/lock-threads.yml`, and a PAT without `workflow` scope may reject the push.

- [ ] 2026-08-27 — **CI has never run 240 of the suite's 539 tests** — `.github/workflows/test.yml:25` runs `node --test tests/agents/*.test.mjs`, but `npm test` is three globs: `tests/agents/*` + `tests/repos/*` + `tests/*`. The other two have never executed on a push or a pull request. Measured on `main` at tonight's clone, all green locally on Node v24.18.1: `tests/agents` **299**, `tests/repos` **86**, `tests/*.test.mjs` **154**. So CI covers 55% of the suite, and the 45% it does not cover is where a lot of recent work landed — `tests/candidates-ttl.test.mjs`, `tests/clipping-write.test.mjs`, `tests/foundry-*.test.mjs`, `tests/sofie-watch.test.mjs`, `tests/static-lint.test.mjs`, `tests/pin.test.mjs` and every `tests/repos/` file. Two of the three fixes shipped on 2026-08-25 put their regression tests in that blind spot. This also means the green-CI signal on every nightly branch to date says nothing about `lib/repo-runtime/**` or `scripts/**`. **Blocked on Jay, not on work**: the nightly PAT has no `workflow` scope, so the runner cannot push `.github/workflows/`. The patch is three added steps, written to `workflow-patch-Agentic-KB-2026-08-27.diff` in the 2026-08-27 nightly outputs and verified with `git apply --check`. Note the fuzzer and context-snapshot steps below it are already inside the `tests/agents/*` glob and run twice today — left alone, since collapsing them is a separate judgement about the advisory framing.
  - 2026-08-30 — **re-issued as a single combined patch with the Node bump, because they are the same file and two separate diffs is friction that has cost six nights.** `workflow-patch.diff` in tonight's nightly outputs changes exactly two things in `.github/workflows/test.yml`: `node-version: 20` → `24`, and `- run: node --test tests/agents/*.test.mjs` → `- run: npm test`. Verified with `git apply --check` against `main` at `bced83d4`, and the result parses as YAML. Deliberately narrower than the 08-27 patch, which added three steps: `npm test` already *is* the three globs, so one substitution covers the whole gap and there is nothing to keep in sync afterwards. Re-measured tonight on Node v24.18.1: `npm test` on `main` is **542 pass / 0 fail** across 34 suites, of which the `tests/agents/*` glob CI runs today is 25 of the 43 files. Note the sharpening since 08-27: the regression tests on the 08-27, 08-28 and 08-29 nightly branches all live in `tests/*.test.mjs`, so **merging those branches would not cause CI to run a single one of their tests**. The blind spot is now where the nightly loop's own output lands.
- [ ] 2026-08-27 — **`confidence` is documented as `string|number`, and the one numeric value that carries meaning is the one value that cannot survive the pipeline** — `promotion-scorer.mjs`'s JSDoc declares `@param {string|number} conf` and `resolveConfidence` implements the numeric pass-through (`typeof conf === 'number'` → clamp). Every other link in the chain then uses `||`, which cannot distinguish `0` from absent. Three sites, all reproduced: (a) `lib/agent-runtime/promotion-scorer.mjs:~137` — `breakdown.trust = Math.min(1.0, baseClassWeight * (breakdown.confidence || 0.5))`, so a candidate whose confidence resolves to `0` is scored on the trust dimension as if it were `0.5`. Probe with `memory_class: learned`, `evidence_count: 2`: `confidence: 0` → `breakdown.trust 0.40`, `confidence: 'unverified'` (0.30) → `breakdown.trust 0.24`. An explicit zero therefore scores *strictly better* on trust than "unverified" — an ordering inversion inside a governance score. (b) `lib/agent-runtime/promotion.mjs:266` — `confidence: item.meta.confidence || 'medium'` upgrades an explicit `0` to **medium (0.75)**. (c) `lib/agent-runtime/bus.mjs:32,94` do the same on the publish side (`confidence || 'medium'`, `confidence || null`). **Currently latent, and that is why it is filed rather than patched**: (c) means no shipped writer can get a numeric `0` into a bus item's frontmatter, so (a) and (b) are reachable only by a hand-edited bus file or a direct `scorePromotion` call. Confirmed reachable in principle — `parseFrontmatter` on `confidence: 0` yields the *number* `0`, not the string. The fix is not four `??`s sprayed across three modules; it is to decide whether numeric confidence is supported at all, and either delete the pass-through from `resolveConfidence` (and drop `string|number` from the JSDoc) or normalise once at the bus boundary so the downstream `||`s never see it.

- [ ] 2026-08-26 — **The active-task pointer and its working-memory file are a two-file state machine with no invariant, and this is the sixth same-class defect** — filed as a design proposal rather than a patch, per the whack-a-mole rule. `lib/agent-runtime/task-lifecycle.mjs` has taken **five** prior nightly fixes in exactly this class: `ebf45935` (repairTaskState cleared the pointer it had just rebuilt), `61ac1c86` (repair pointer writes were not atomic), `875865c6` (abandonTask clobbered other tasks' state), `52cdcbf5` (startTask silently orphaned an in-flight task), `5232294c` (startTask clobbered archived working memory). Each one repaired a different illegal transition of the same pair. The sixth was found and deliberately **not** patched.
  - The sixth, reproduced: `verifyTaskState` reports `active-pointer-task-mismatch` with `repairable: true`, and `repairTaskState` has no branch that resolves it. Probe — one working-memory file `task-A.md` with `status: active`, and a pointer with `status: active`, `working_memory: …/task-A.md`, `task_id: task-B`. Result: `verifyTaskState` → `ok: false`, one issue, `repairable: true`; `repairTaskState` → `repaired: false`, `actions: []`, `ok: false`, same issue still present. The declared repairability is a lie, and it is a lie on every subsequent run.
  - Why it matters: the agent is wedged with no documented way out. `startTask` throws "already has active task task-B"; `abandonTask('task-B')` throws "working-memory not found for task task-B" (no file carries that id); `abandonTask('task-A')` *succeeds* but does **not** clear the pointer, because it only clears when `pointer.task_id === taskId` — so the pointer survives as `status: active, task_id: task-B` and `getActiveTask` still reports an active task. The only escape is the two-step `abandonTask('task-A')` then `repairTaskState()`, which works solely because the first step changes the failure into `active-pointer-target-not-active`, a *different* issue code that repair does handle. Nothing tells a caller that.
  - The one-line fix is obvious and is the reason this entry exists instead of a commit: `repairTaskState`'s rebuild branch already writes `task_id: wmMeta.task_id`, which would resolve the mismatch — it simply is not entered, because its guard is `pointerMeta.working_memory !== activeFile.path` and here the paths agree. Adding `|| pointerMeta.task_id !== wmMeta.task_id` closes this one case and leaves the shape that generated all six.
  - The proposal: there is no single function that states the legal `(pointer, working-memory)` states, so every entry point — `startTask`, `abandonTask`, `closeTask`'s seal path, `repairTaskState` — re-derives consistency by hand and each has been patched separately for a case the others handle. Give the pair one owner: a `transition(from, to)` over the enumerated legal states (the repo already has `lib/agent-runtime/state-machines.mjs` doing exactly this for bus items, so there is a pattern to copy), make `verifyTaskState`'s issue codes the state names, and derive `repairable` from whether a legal transition to a consistent state exists rather than hardcoding `true` per issue. Then `repairable: true` cannot disagree with `repairTaskState` again, because both read the same table. Expect this to surface further illegal states currently reachable — that is the point of doing it once with a fixture rather than a seventh night of one-line guards.

- [ ] 2026-08-26 — **The write guard lets a caller-supplied variable value change a rule's glob semantics, and a deny rule with an unresolved variable fails open** — two defects in `lib/agent-runtime/paths.mjs`, which has **zero prior nightly commits** (it is untouched scaffold code), found while auditing the `assertWriteAllowed` chain. Not patched: both share one root cause and the correct fix is structural, not a patch to either symptom.
  - (a) **Glob widening via a variable value.** `expandVars` substitutes into the *pattern string*, and `globToRegex` then interprets the result — so metacharacters inside the substituted value are read as glob syntax. Reproduced against `planning-agent`'s real rule `wiki/projects/{{project}}/decisions.md`, target `wiki/projects/SOMEONE-ELSES-PROJECT/decisions.md`: `project: "my-project"` → denied (`not in allowed_writes`); `project: "*"` → **allowed**; `project: "**"` → **allowed**. The value is caller-controlled and unvalidated — `closeTask` does `vars = { project: payload.project || null }` straight from the MCP tool argument / route body, and the MCP schema is a bare `{ type: 'string' }` (contrast `mcp/server.js:1167`, which does run `validateSlug` on `args.project` for the rewrite path). **Currently latent, and that is the only reason this is not urgent:** `_planWrites` derives every file path from `contract.agent_id`/`contract.tier`, so no planner emits a `wiki/projects/{{project}}/…` path today and the widened rule is never the rule that decides a write. It becomes live the moment any planner emits a project-scoped path.
  - (b) **A `forbidden_paths` rule with an unresolved variable denies nothing.** When a var is missing, `expandVars` leaves the literal `{{name}}`; `globToRegex` escapes `{` and `}` as regex literals, so the rule matches no real path. For `allowed_writes` that is fail-closed and fine. For a deny rule it is fail-open. Reproduced: contract with `allowed_writes: ["wiki/**"]` and `forbidden_paths: ["wiki/projects/{{secret}}/**"]`, called with empty vars, writing `wiki/projects/classified/notes.md` → `allowed: true, reason: "matched allowed_writes"`. No shipped contract puts a variable in `forbidden_paths` today, so blast radius is currently zero.
  - Why not patched tonight: the obvious fix — escape metacharacters in the substituted value — **cannot work**, because `globToRegex` has no escape mechanism. It converts `\` to a literal backslash in the output regex, so `\*` becomes `\\` followed by `[^/]*` and matches *more*, not less. `[*]` fails the same way. There is no string `expandVars` can emit that `globToRegex` will read as a literal asterisk. The real fix is to stop expanding into the pattern string at all: build the regex in one pass (`globToRegexWithVars(pattern, vars)`) that glob-converts the literal chunks and `RegExp`-escapes the substituted values, and return the set of unresolved variable names so the caller can fail closed on a deny rule. That replaces `expandVars`+`globToRegex` at all four call sites (`paths.mjs` ×2, `context-loader.mjs:93`, `vault-writeback.mjs:30`) — the central write chokepoint, which wants a measured blast radius and a fixture, not a 3am rewrite. Note (b) additionally needs a policy call from Jay that is not a code question: an under-specified deny rule failing closed means it blocks *every* write until the variable is supplied.

- [ ] 2026-08-31 — **`learned` is `classFor`'s catch-all, so a `{ class: 'learned', scope: 'repo' }` rule still swallows the whole `repo-docs/` GitHub mirror** — the narrowed remainder of the 2026-08-25 `classFor` entry, whose bus half closed tonight (`94223c85`). What is left is the part that was never about bus items: every path under `wiki/repos/<repo>/` that matches none of the eight explicit rules returns `learned`, so one `class: learned` include rule admits the read-only synced mirror alongside the agent's actual gotchas, until the byte budget runs out. Two things have to be settled together and neither is a one-line change. (a) `memory-classes.mjs:17` claims "Explicit frontmatter overrides this" and **nothing in either context loader implements it** — `resolveRepoIncludeRule` compares `classFor(path)` against `rule.class` and never reads the file's `memory_class`, even though `publishRepoBusItem` and `readOrInit` both stamp it. Honouring frontmatter first is the principled fix, but it turns a pure path predicate into a file read for every candidate in the walk. (b) Failing that, `classFor` needs a class that means "not classified" so `learned` stops being load-bearing in two roles at once, which is a signature change across `writeback.mjs`, `repo-runtime/writeback.mjs` and both loaders. Tonight's dedup commit (`c3d2ca7a`) removes the double budget charge that made this worse but not the over-admission itself.


- [ ] 2026-08-25 — **`scoreFreshness` fails open on an unreadable file, and `inferClass` matches by substring** — `lib/agent-runtime/freshness.mjs:105` returns `0` when `statSync` throws, commented "can't determine age, treat as fresh", which makes `decayScore` return `1.0` and `isFreshForCanonical()` return **true** for a file that does not exist. A promotion gate whose purpose is rejecting stale evidence therefore passes evidence that is missing entirely, and `getAgeInDays` has no way to signal "unknown" so the caller cannot tell. Separately, `inferClass` (line 121) uses `String.includes('/profile.md')` where `memory-classes.mjs:classFor` anchors with `$`, so `profile.mdx`, `hot.mdx` and `profile.md.bak` all match — and both `profile` and `hot` are `{ exempt: true }`, hardcoding score `1.0` forever. `.mdx` is live here: `repo-runtime/sync.mjs` imports `*.mdx` and `context-loader.mjs` walks it.
  - 2026-08-28, correction to this entry's second half — **`inferClass` and `classFor` are not twins, so "anchor it like `classFor`" is not a valid acceptance criterion, and this half is not the one-run fix it has been described as.** Three consecutive `notes-to-factory` runs carried it in run state as "genuinely Small and takeable on its own — say the word and it is a one-run fix". Retracting that, having now read both functions rather than the entry describing them. Two findings, both verified against `origin/main`:
    - **Different vocabularies, deliberately.** `inferClass` (`freshness.mjs:121`) returns `profile|hot|working|learned|personal|session|canonical` and catches all to `session`; `classFor` (`memory-classes.mjs`) returns `profile|hot|working|learned|rewrite|bus` and catches all to `learned`. Three of `inferClass`'s classes do not exist in `classFor` and two of `classFor`'s do not exist in `inferClass`, and they disagree on purpose about `^wiki/system/bus/` — `classFor` says `bus`, `inferClass` says `session`. `inferClass` keys `FRESHNESS_PROFILES`; `classFor` keys `CLASSES` (retention, append-only). They are two tables for two purposes that happen to share five key names. So a test asserting `inferClass(p) === classFor(p)` would be wrong to write, and "make them agree" is not the fix.
    - **Reachability is zero on the tracked tree, not merely latent.** `git ls-tree -r --name-only origin/main | grep '/profile\.md' | grep -v '/profile\.md$'` → no matches; same for `hot.md`; and `grep -c '\.mdx$'` over the same listing → **0**. No `.mdx` file is tracked in this repo at all. The entry's "`.mdx` is live here" is true of the *sync path* (`sync.mjs:11,13,340` and `context-loader.mjs:21` do handle `.mdx`), but that path mirrors external repos into `wiki/repos/<repo>/repo-docs/`, so reaching this defect needs some third-party repo to contain a file whose path matches `/profile.md*`. That is not "live", it is hypothetical.
    - What is actually left: `profile` and `hot` are confirmed `{ exempt: true }` (`freshness.mjs:22-23`), so a matching path would be exempt from decay forever, and `String.includes` is genuinely the wrong operator. But the open question is not `includes` vs `$` — it is what the exemption is *for*. If it means "this is an agent's own profile page", the test wants anchoring **and** scoping (`^wiki/agents/.*/profile\.md$`), not a bare anchor that still matches `wiki/repos/x/repo-docs/profile.md`. That is a small design call for Jay, which is why this stays Open and unimplemented rather than becoming a two-character edit. The first half of this entry (`scoreFreshness` fail-open) is unchanged and still the policy question it always was.
    - Verified by `notes-to-factory` 2026-08-28. No code changed.

- [ ] 2026-08-25 — **`recordApiCall` swallows write failures, disabling the daily spend cap silently** — `lib/agent-runtime/cost-meter.mjs:53` is a bare `catch {}` around the `appendFileSync` to `logs/api-cost.log`, and still returns the computed cost to the caller as though it were recorded. `readCostLog` then drops any unparseable line (there is no lock, and the MCP server, CLI and web routes all append concurrently, so interleaved partial lines are possible). `assertWithinDailyCap` computes `spendForDay(...) === 0 < cap` and lets every call through; `summary()` reports `today_usd: 0`. A read-only or full `logs/` directory turns the module header's "hard cap" into no cap at all, with no signal anywhere. Wants a fail-closed decision from Jay — refusing to spend when spend cannot be recorded is the safe reading but it is a policy call, not a patch.

- [ ] 2026-08-25 — **A second, independent clipping writer bypasses `clipping-write.mjs`'s dedup, and has already produced 11 duplicate copies of one note** — `scripts/lib/clipping-write.mjs` implements identity correctly: `canonicalHash` uses `sha256(source + "\0id\0" + sourceId)` when `--source-id` is supplied, which is immune to both Apple Notes' timezone-less timestamps and HTML→text whitespace drift. But `grep -rln clipping-write scripts cli lib` finds only two references in the whole repo — the module itself and `scripts/foundry-finalize.sh`. The Apple Notes `KB Inbox` capture path, `scripts/sofie-watch-obsidian.mjs`, references neither `clipping-write` nor `source-id`; it writes clippings through its own path with content-hash identity. The result is recorded in this morning's `wiki/log.md` entry: the single stale `test-capture-2026-05-16` note "already has 11 duplicate copies in `raw/clippings/` from the known write-time hash drift", and the morning-review job now deliberately declines to re-capture it — a scheduled job routing around a defect rather than the defect being fixed. `ls raw/clippings/ | grep -c test-capture` confirms 11. This is the same shape as the frontmatter-codec item above (four independent codecs, none routing through the canonical one), and it wants the same fix: make there be one clipping writer. Two halves, and the second is not a code question — (a) route `sofie-watch-obsidian.mjs` through `clipping-write.mjs` with the Apple Notes `x-coredata://` id as `--source-id`, plus a regression test that writes the same note twice and asserts one file; (b) decide what happens to the 11 existing duplicates, which is a governance call for Jay, not a nightly patch — `raw/` is immutability-governed (Rule 1, "nothing is destroyed"), so deleting ten of them contradicts the invariant while leaving them keeps a known-bad corpus that every compile pass re-reads. Sized Medium-to-Large on (a) alone because it changes a scheduled capture path's on-disk identity scheme, and blocked on (b) regardless. Not implemented. Source: KB `wiki/log.md` 2026-08-25 morning-review-daily entry, "KB capture (Step 2)".

- [ ] 2026-08-24 — **Two nightly branches are open**: `nightly/2026-08-24-improvements` (1 commit, the promotion-provenance fix) and `nightly/2026-08-25-improvements` (2 commits, the `@anthropic-ai/sdk` bump and the `.nvmrc` Node bump — see the Node entry below, which the second one is half of). Recorded for continuity; two is still the routine state, not the pileup that dominated this file on 08-21/08-22. Both are Tier B by policy (dependency and version changes), so neither can self-merge.
  - 2026-08-26 — now **three**: `nightly/2026-08-26-improvements` adds the `next` 16.3.3 security bump. Worth a note on merge order rather than just a count. The 08-26 branch is the one with a clock on it — it is two unauthenticated-RCE advisories — and it touches only `web/package.json` + `web/package-lock.json`. The 08-25 branch also touches `web/package.json` (the `@anthropic-ai/sdk` line) and `web/package-lock.json`, so **these two will conflict in the lockfile if merged independently**. Merging 08-26 first is the right order: it is the security one, it is the smaller diff, and rebasing the SDK bump onto it is a `npm install` re-resolve rather than a hand edit. Three is drifting back toward the pileup this file has twice recorded as the thing that blocks everything else.
  - 2026-08-27 — now **four**. All three above re-verified unmerged at tonight's clone (`git merge-base --is-ancestor` false for each), and `nightly/2026-08-27-improvements` adds a fourth (one commit, the `parseCompileLog` prose-section fix). The fourth is the one that does *not* add to the merge-order problem: it touches only `scripts/lib/foundry-propose-core.mjs` and `tests/foundry-propose.test.mjs`, which no other branch and no other `main` job touches, so it can merge in any order without conflict. The 08-25/08-26 lockfile conflict recorded above is unchanged and still the thing to sequence — 08-26 first. Four is the pileup, not a drift toward it.
  - 2026-08-28 — now **five**. `nightly/2026-08-28-improvements` adds two commits, both `scripts/lib/clipping-write.mjs` + `tests/clipping-write.test.mjs`. Like the 08-27 branch it does *not* add to the merge-order problem — no other branch and no `main` job touches either file — so it can merge in any order. The 08-25/08-26 lockfile conflict is unchanged and still the thing to sequence: **08-26 first**. Five branches is the pileup compounding, and the two oldest are now eight and four nights old respectively.
  - 2026-08-29 — now **six**. `nightly/2026-08-29-improvements` adds one commit (`scripts/candidates-ttl.mjs` + `tests/candidates-ttl.test.mjs`). Like the 08-27 and 08-28 branches it does *not* add to the merge-order problem — no other branch and no `main` job touches either file — so it merges in any order. The sequencing constraint is unchanged and is still only between two branches: **08-26 first, then 08-25**, because both edit `web/package-lock.json` and the 08-26 one is the two-CRITICAL Next.js bump with a clock on it. Six unmerged branches is now the dominant fact about this repo's nightly loop: every one of them is green, small, and single-purpose, and the work is not compounding while they sit.
  - 2026-08-30 — now **seven**. `nightly/2026-08-30-improvements` adds two commits, both `scripts/scrub-raw-pii.mjs` (+ `scripts/hooks/pre-commit`, + `tests/scrub-raw-pii.test.mjs`). No other branch and no `main` job touches those files, so it merges in any order and does not add to the merge-order problem. **The severity spread across the seven is not flat and should drive the order**: `nightly/2026-08-26-improvements` is `next` 16.3.1 → 16.3.3 for two CRITICAL unauthenticated RCEs, and `main`'s `web/package-lock.json` still resolves `next` at **16.3.1** — verified tonight by reading the lockfile on both refs. That is a live, four-nights-old exposure sitting in a branch, and it is the only one of the seven where waiting has a cost measured in something other than tidiness. Everything else here is a correctness fix that can wait indefinitely.
  - 2026-08-31 — now **eight**. `nightly/2026-08-31-improvements` adds three commits (`lib/repo-runtime/context-loader.mjs`, `lib/agent-runtime/memory-classes.mjs`, `lib/agent-runtime/identity.mjs`, plus their tests). No other open branch and no `main` job touches those files, so like the 08-27/08-28/08-29/08-30 branches it merges in any order and does not add to the merge-order problem. **The clock on `nightly/2026-08-26-improvements` is now five nights.** Re-derived from the tracked manifests tonight rather than taken from the brief: `web/package.json` declares `"next": "^16.2.6"` and `web/package-lock.json` on `main` still resolves `node_modules/next` at **16.3.1**, with `sharp` at 0.35.3. The 16.3.3 bump that fixes it is committed, green and sitting unmerged on that branch (`497ebcc`). Nothing was re-attempted here tonight — duplicating a fix that already exists on a branch is how this repo ends up with two of them. **This is the single highest-value action available to Jay and it is a merge, not a code change.**

- [ ] 2026-08-20 — **Replace the hand-rolled frontmatter codec with the `yaml` package** — `lib/agent-runtime/frontmatter.mjs` has now taken five nightly fixes for the same class of defect (`693f718a` CRLF, `5c5c17b8` round-trip fidelity, `43465c7e` YAML-illegal scalars, `9a2a4fbc` duplicate block on CRLF, and this run's BOM + hyphenated-key fix). Each one is a new way the 60-line parser disagrees with real YAML. The root package already depends on `yaml@^2.8.3` and `web/` already uses `gray-matter`, so the dependency cost is zero. The work is a swap behind the existing `parseFrontmatter`/`serializeFrontmatter`/`updateFrontmatter` signatures plus a corpus round-trip test over `wiki/**`. Until that lands, expect a sixth encoding bug.
  - 2026-08-22, scope correction — **swapping `frontmatter.mjs` alone will not close this class.** `lib/agent-runtime/frontmatter.mjs` is not the only frontmatter parser in the repo; there are at least three more, each an independent regex with its own bugs, and none of them route through it:
    - `scripts/lib/compile-gate-core.mjs:16` — `extractFrontmatter`, `/^---\n([\s\S]*?)\n---/`. No CRLF and no BOM handling, so on either input it returns `''` and the 2-source compile gate sees a summary with **zero themes** — it does not error, it silently declines to count that source. `extractKeyConcepts` two functions down only matches the single-line `key_concepts: [a, b]` form, so the ordinary multi-line YAML list silently yields `[]` (documented in the docstring, but the caller cannot tell "no key concepts" from "written as a block list").
    - `scripts/scrub-raw-pii.mjs:180` — `stampFrontmatter`, same regex, same blind spots. Here the failure writes: a CRLF or BOM-prefixed `raw/` capture gets a **second** frontmatter block prepended, which is exactly the bug `9a2a4fbc` and `e55158a1` already fixed twice in `frontmatter.mjs`.
    - `web/` uses `gray-matter` — a fourth codec with a fourth set of behaviours.
    - 2026-08-27, one more for the enumeration — `scripts/sofie-sync-memory.mjs:49` is a **fifth**: `profileContent.indexOf('\n---', 4)` plus a `startsWith('---\n')` guard, hand-inlined rather than imported. It is the least harmful of the five because it is fail-*closed*: a CRLF or BOM-prefixed `profile.md` fails the `startsWith` guard and the script exits 1 with "profile.md missing frontmatter" rather than writing a second block the way `scrub-raw-pii.mjs` does. Adding it to the list only — no sixth same-class patch was attempted, per the whack-a-mole rule, and it does not change the design proposal, which is still "make there be one parser".
    Deliberately **not** fixed as a sixth same-class patch (see the whack-a-mole rule). The design point is that the fix is not "make the parser better", it is "make there be one parser": export the codec from `lib/agent-runtime/frontmatter.mjs`, delete the three regex copies, and make the corpus round-trip test the gate for all of them. Until the callers are unified, a `yaml`-package swap behind `frontmatter.mjs` moves the floor without raising it.

- [ ] 2026-08-20 — **One fail-closed `requireAuth()` chokepoint for the web write routes** — auth is currently re-implemented per route and fails *open*. `/api/process`, `/api/compile` and `/api/lint` gate on `if (PRIVATE_PIN && !pinMatches(...))`, so an unset `PRIVATE_PIN` means no auth on routes that spend the Anthropic key and write the wiki — while the read routes (`/api/search`, `/api/article`) correctly treat an unset PIN as "private disabled". Worse, these have **no check at all**: `POST /api/agents/promote`, `POST /api/agents/bus/[channel]`, `POST /api/ingest`, `POST /api/repos`, `POST /api/repos/[repo]/sync`, `POST /api/process/run-all`, and both `close-task` routes. `resolveIdentity` in `lib/agent-runtime/identity.mjs` compounds it: the `X-Identity-Kind`/`X-Identity-Id` and `X-Agent-Id` branches are unverified *and* are checked before the bearer-token branch, so any caller can name themselves `sofie` and have that id signed into the audit chain. There is no `middleware.ts`. Patching routes one at a time is how this became nine routes; the fix is a single helper plus a test that enumerates every non-GET handler.

- [ ] 2026-08-20 — **Retire the regex denylist in `scripts/hooks/pre-commit` for a real secret scanner** — the hook's whole deny-list is PII patterns; it has *no* pattern for API keys, tokens, PATs or passwords, so `ANTHROPIC_API_KEY=sk-ant-…`, `ghp_…` and `AKIA…` all commit cleanly through it. Meanwhile the PII half is on a treadmill: `69917a6d` (PII guard silently disabled the Rule 13 vault guard), `42b14fcf` (therapy patterns loosened to unblock a public quote), `3afe01c5` (unjam a raw/ PII deadlock that blocked 41 Refinery runs) — three loosening commits in five days, and the one real credential leak of the period (`f0a12403`, a bearer-token prefix persisted into committed frontmatter) sailed past it and had to be fixed in application code. Structural bypasses: the scan uses `--diff-filter=AM`, so a `git mv` carries staged PII through untouched; base64 defeats it entirely; and `core.hooksPath` is unset in a fresh clone so it does not run at all. Proposal: `gitleaks`/`trufflehog` over `--diff-filter=ACMRT` with an allowlist file, keep only the vault-path guard as bespoke logic, and make installation part of `npm ci`. **Do not add pattern #25.**

- [ ] 2026-08-20 — **Escape the JS heredoc in `scripts/team-setup.sh`** — lines 341-365 interpolate `$PRIVATE_PIN`, `$KB_DIR`, `$WIKI_DIR` and `$OPENAI_API_KEY` directly into JavaScript source piped to `node`. Any of those values can close the string literal and execute arbitrary code; the values come from interactive `read -r` prompts and the script advertises a `curl … | zsh` install. Pass them via `process.env` or `process.argv` instead of string interpolation. Related, same file: `PRIVATE_PIN="${PRIVATE_PIN:-changeme}"` persists a guessable default into `web/.env.local`, the LaunchAgent plist, and the Claude Desktop MCP config.

- [ ] 2026-08-20 — **Raise the PIN work factor in `scripts/pin.mjs`, and treat the committed `.enc` blobs as already exposed** — the construction is sound (AES-256-GCM, per-file random salt and IV, auth tag verified, tmp+rename), but `PBKDF2_ITER = 100_000` SHA-256 with a 4-character minimum is not a work factor. Measured in this sandbox at ~116 guesses/sec/core, the entire 4-digit keyspace falls in about 86 seconds. `wiki/_private/.enc/stuck-on.md.enc` and `next-milestone.md.enc` are **committed to a public repo**, so this is an offline attack on anyone who clones. Switch to scrypt or argon2id, enforce a real minimum length, and re-lock every blob under a new PIN.

- [ ] 2026-08-20 — **`atomicWrite` is copy-pasted nine times and none of the copies fsync** — `writeback.mjs:228`, `repo-runtime/writeback.mjs:20`, `retention.mjs:13`, `sync.mjs:205`, `vault-writeback.mjs:42`, `promotion.mjs:41`, plus inline copies in `bus.mjs`, `repo-runtime/bus.mjs`, `registry.mjs` and `task-lifecycle.mjs`. All are `writeFileSync(tmp)` + `renameSync`. Without an fsync on the file and on the containing directory, a power loss can make the rename durable while the data blocks are not — strictly worse than a plain overwrite for the "must never leave a truncated registry.json" goal the code states. None of them unlink the tmp file if the rename throws, so an ENOSPC leaves `.tmp-…` orphans that `walkDocs`/`walkDir` filter out (they match `.md`) but git and Obsidian do not. One shared helper in `lib/agent-runtime/atomic.mjs`.
  - 2026-08-21, scope correction: `grep -rn renameSync lib scripts cli` finds **34** tmp+rename sites, not nine — `task-lifecycle.mjs` alone has six, and `scripts/` has another twelve (`generate-stats`, `static-lint`, `backfill-ids`, `candidates-ttl`, `sofie-*`, `graph-maintenance-scan`, `audit-context-leaks`) plus four in `cli/kb.js`. Sequence the helper before the migration and land the migration in per-directory commits; a single 34-file commit cannot be reviewed. Note also that `promotion.mjs` and `cli/kb.js` are touched by the unmerged 2026-08-20 branch, so the top item in this file gates part of this one too.
  - 2026-08-24 — **unblocked.** All three nightly branches were drained into `main` on 2026-08-23 (`425fb7cd`, `7b307362`, `dd457bfb`; all twelve commits verified as ancestors of `main` and the remote branches deleted). `promotion.mjs`, `cli/kb.js`, `frontmatter.mjs`, `locks.mjs` and `lib/repo-runtime/queries.mjs` are all on `main` now, so this item, the frontmatter-codec item and the `requireAuth()` chokepoint item no longer conflict with anything unmerged. Nothing else about their scope changed.

- [ ] 2026-08-20 — **Node 20 is EOL (2026-04-30) and is pinned in two places** — `.nvmrc` says `20` and `.github/workflows/test.yml` sets `node-version: 20`. Bumping only `.nvmrc` desyncs CI from local, so both must move together; the workflow half needs a token with `workflow` scope, which the nightly runner does not have. This has now been Open for four nights on that one blocker.
  - 2026-08-25 — **half-landed, and the other half is a one-line patch waiting on Jay.** `.nvmrc` → `24` is committed on `nightly/2026-08-25-improvements`. It is deliberately *not* on `main`: on its own it would desync CI from local, which is the exact failure this entry warns about, so it must merge together with the workflow change. The workflow patch is a single line (`node-version: 20` → `24` in `.github/workflows/test.yml:18`) and was written to `workflow-patch-Agentic-KB-2026-08-25.diff` in the 2026-08-25 nightly outputs, verified with `git apply --check`. **Action for Jay: apply that one-line patch and merge the branch in the same PR.**
  - Correction to the previous note, which said "Node 22 is the active LTS; the suite passes on Node 22 today (it is what the nightly runs on)". Both halves are now stale. As of 2026-08, Node 22 is *Maintenance* LTS (to 2027-04) and **24 is Active LTS** (to 2028-04). And the nightly does not run on 22 — the host has `node v24.18.1` on `PATH`, so `.nvmrc: 20` has been describing a runtime nobody uses. The 24 target was chosen because it is what was actually verified this run (root suite 536/536, `web/` `tsc --noEmit` clean, `next build` clean, all on v24.18.1) and because it aligns the pin with the machine. Node 22 could not be verified: the local `node@22` brew install is broken (`Library not loaded: libsimdjson.30.dylib`), so a 22 target would have been an unverified guess. If Jay prefers 22 for conservatism, that is a defensible call — but it needs a working 22 to verify against first.
  - Follow-on, not blocking: `web/devDependencies` still pins `@types/node: ^20`. Nothing currently uses a Node 22+ API so it typechecks fine, but the types should follow the runtime whenever someone is in that file.
  - 2026-08-26 — **the missing Node 22 verification now exists, and the "aligns the pin with the machine" argument for 24 no longer holds.** The 08-25 note recorded that 22 "could not be verified" because the local `node@22` brew install was broken, and chose 24 partly because the host ran v24.18.1. Tonight the nightly host resolves `/usr/local/bin/node` → **v22.14.0**, and the full root suite passes on it: `npm ci --include=dev` clean, `npm test` **536 pass / 0 fail**, plus `web/` `npx tsc --noEmit` and `next build` both clean on the same runtime. So *both* 22 and 24 are now verified-green here, and the machine argument now points at 22 rather than 24. This does **not** change the recommendation on its own — 24 is still Active LTS to 2028-04 and 22 is only Maintenance to 2027-04, which is the stronger reason — but it removes the last blocker on choosing 22 if Jay prefers conservatism, and it means whichever number is picked, `.nvmrc` should be treated as a deliberate choice rather than a description of the host, because the host has now changed under this entry twice in two nights. **Nothing was changed tonight**: the pin still says `20` on `main`, the `24` commit still sits unmerged on `nightly/2026-08-25-improvements`, and the workflow half is still the one-line patch waiting on Jay. Six nights Open on that one blocker.

- [ ] 2026-08-20 — **`web/` has no test suite** — 41 route handlers and 20 lib modules, zero tests. `npx tsc --noEmit` and `next lint` are the only gates, so every web-side fix this repo has shipped (six webhook fixes alone) was verified by inspection or by hand-driving `next dev`. A thin `node --test` harness that imports the route handlers directly and drives them with a synthetic `NextRequest` would have caught the `X-KB-Namespace` bypass fixed this run.

## Closed

> **Read this before trusting a Closed entry: "Closed" here has meant "a fix was
> written", not "the fix is on `main`."** Corrected 2026-09-01. The three
> 2026-08-31 entries below were filed with commit SHAs — `3229eac9`, `2330744c`,
> `ec3484c1` — that exist in **no** object in this repository. The 08-31 run
> recorded them into the backlog on `main` from its pre-push local branch, then
> pushed the branch under different SHAs, so the citations were dead on arrival
> and a future run following them would have found nothing. The real commits are
> `94223c85`, `c3d2ca7a` and `f104dad2`, and **all three are on
> `nightly/2026-08-31-improvements`, which is still unmerged** — none of these
> three fixes is on `main`. Verified 2026-09-01 with `git cat-file -t` over every
> SHA cited anywhere in this file: those three were the only unresolvable ones,
> and `c573cf06`/`6858fdd3` (2026-08-30 PII scrubber) resolve but are likewise
> branch-only. Cite a SHA only after it is an ancestor of `main`, or say which
> branch carries it.

- [x] 2026-08-25 → 2026-09-01 — **`closeRepoTask` silently ignored `payload.dryRun`, while its agent-runtime twin honoured it** — fixed in `c17a0478`, **on `nightly/2026-09-01-improvements`, not on `main`** (stated explicitly per the note above). `lib/agent-runtime/writeback.mjs:296` short-circuits `closeTask` on `dryRun === true`; `lib/repo-runtime/writeback.mjs:247` had no such branch, so `dryRun` was an unrecognised key and the call took the per-repo lock and did the whole real close. Reproduced on a fixture KB before the fix: the return value was `{ ok, trace, committed: true }` with **no `dryRun` key at all**, `progress.md` written and containing the dry-run text, `hot.md` written, and 1 bus discovery item published — so a caller asking for a preview got irreversible writes and nothing in the response said so. After: `{ ok, dryRun: true, wouldSucceed, planned, rejected, summary }`, nothing on disk, zero bus items. The fix is the five-line mirror of the twin, dispatching to `dryRunCloseRepoTask`, which already existed at line 358 and was simply unreachable through this entry point; `ok` tracks the plan (`plan.wouldSucceed !== false`). Two regression tests in `tests/repos/repo-runtime.test.mjs`, both verified failing against pre-fix source (source change stashed → 2 fail / 2 pass; restored → 4 pass). Full suite 604/604 on Node v24.18.1.

- [x] 2026-08-25 → 2026-08-31 — **`classFor()` does not recognise repo-scoped bus paths** — fixed in `94223c85`: `classFor` now returns `bus` for `^wiki/repos/[^/]+/bus/`, anchored at exactly one repo segment so the existing assertion that `wiki/repos/x/wiki/system/bus/item.md` is *not* bus still holds and a synced doc under `repo-docs/bus/` still falls through to `learned`. Reproduced before the fix with a fixture KB and a `{ class: 'learned', scope: 'repo' }` rule: `loadRepoContext` returned the one bus item **twice** — once at priority 30 with reason `repo bus item` from the step-5 sweep, once at priority 70 as `repo class=learned scope=repo` — and charged its bytes against `budget_bytes` twice. Both entries are gone; the item is delivered once, as `bus`. The mirror case (`{ class: 'bus', scope: 'repo' }` matching zero files, so its `status` filter was a silent no-op) is fixed by the same line. No op-level change: `isAppendOnly` is false for both classes and nothing outside the tests calls `retentionDaysFor`. The double-charge half needed its own fix and got one — see `c3d2ca7a` below. The entry's third consequence, `learned` over-admitting the whole `repo-docs/` mirror, is **not** closed and is re-filed narrowly at the top of Open. Regression tests: `tests/agents/memory-classes.test.mjs` (`repo-scoped bus items are bus, not learned`, fails pre-fix with `actual: 'learned'`).

- [x] 2026-08-31 → 2026-08-31 — **`loadRepoContext` had no dedup, so a doubly-matched file was charged against the context budget twice** — found while reproducing the `classFor` entry above; `lib/repo-runtime/context-loader.mjs` pushes from five independent sources (canonical, progress, include rules, `source_files`, the bus sweep) and had no `seen` set, while its agent-runtime twin `loadAgentContext` has guarded exactly this since it was written. Any file two sources resolve — most easily a bus item that the step-5 sweep delivers and an include rule also matches, but equally two overlapping path globs — was pushed twice *and* had `usedBytes += meta.bytes` applied twice, so the phantom charge evicted files that would otherwise have fit and `truncated` could flip true on a bundle that lost nothing. Fixed in `c3d2ca7a` with first-writer-wins, matching the twin; a duplicate is skipped without a `drop` record because it is not a loss. Regression test in `tests/repos/repo-runtime.test.mjs` fails pre-fix with `actual: 2, expected: 1`.

- [x] 2026-08-31 → 2026-08-31 — **`resolveIdentity` rejected a conformant lowercase `bearer` auth-scheme, and fingerprinted the empty string as if it were a credential** — `lib/agent-runtime/identity.mjs:94` matched with `auth.startsWith('Bearer ')`. RFC 7235 §2.1 makes the scheme token case-insensitive and RFC 7230 allows more than one space before the credential, so `bearer <token>`, `BEARER <token>` and `Bearer\t<token>` all fell through every branch to the anonymous fallback — the caller's requests were then attributed to `anonymous` in the audit log and in the committed bus frontmatter that `/api/agents/bus` writes. Second defect in the same expression: `Bearer ` with nothing after it produced `token:e3b0c44298fc`, the fingerprint of `''`, so every caller sending an empty credential collapsed onto one plausible-looking service id. **Severity is attribution, not authorization** — identity.mjs is identification only, `web/src/lib/rbac.ts` does the namespace ACL work and has always used `/^Bearer\s+(.+)$/i`, and both call sites take the identity as a `body.from`/`body.approver` fallback that a caller can override anyway. Fixed in `f104dad2`; fingerprints for well-formed headers are unchanged. Three regression tests in `tests/agents/runtime.test.mjs`, all failing pre-fix.


- [x] 2026-08-22 → 2026-08-30 — **`npm run lint` in `web/` has not run a linter since the Next 16 bump** — both halves of the failure reproduced verbatim before the change: `npm run lint` → `Invalid project directory provided, no such directory: …/web/lint`, exit 1; `npx eslint .` → `ESLint couldn't find an eslint.config.(js|mjs|cjs) file`, exit 2. The fix turned out to be cheaper than the entry's estimate. `eslint@^9.39.5` and `eslint-config-next@^16.2.2` were already in `web/devDependencies`, and `eslint-config-next@16` exports flat-config arrays directly (`Linter.Config[]` at `./core-web-vitals` and `./typescript`), so **no dependency was added and `web/package-lock.json` is untouched** — the whole change is a new `web/eslint.config.mjs` spreading those two arrays and `"lint": "eslint ."`. The predicted flood did not materialise either: the first real run over 41 route handlers and 20 lib modules reports **16 problems, 5 of them error-level across only 3 rules**. Application code was deliberately not touched, so those 3 are downgraded to warnings — `react-hooks/set-state-in-effect` (2: `src/app/agents/page.tsx`, `src/lib/private-mode-context.tsx`), `@typescript-eslint/no-explicit-any` (2: `src/app/repos/[repo]/page.tsx`) and `prefer-const` (1: `src/app/api/repos/[repo]/search/route.ts`) — each named with its occurrences in a comment in the config, to be re-promoted as they are cleaned up. Every other rule keeps its upstream severity and no `--max-warnings=0` was added, so the remaining 11 warnings (8 `no-unused-vars`, 1 `import/no-anonymous-default-export`, and 2 *unused* `eslint-disable` directives — someone wrote suppressions for a linter that then never ran) are visible without being a gate. **The linter was proved to actually fire**, which is the half a passing exit code cannot demonstrate on its own: a scratch component under `src/lib/` with a keyless `.map()` produced `error Missing "key" prop for element in iterator react/jsx-key` and exit 1; deleting it returned the tree to 16 warnings and exit 0. One caveat recorded rather than smoothed over — the probe also assigned to a `const` and `no-const-assign` did *not* report, so ESLint's core recommended set is not wholly enabled by this config; `tsc` catches that particular case, but do not read "lint passes" as "core JS rules are on". Full gate on the change: root suite 543/543 unchanged, `tsc --noEmit` 0, `next build` 0, `npm run lint` 0. Fixed on `ntf/2026-08-30-web-lint` (`a0e2c18`). This also settles the correction the entry carried: `web/` now has two gates rather than one, and may honestly be described as linted.
- [x] 2026-08-30 → 2026-08-30 — **A capture into a *new* `raw/` subdirectory was invisible to the scrubber that exists to unblock it** — `dirtyRawFiles` ran `git status --porcelain -- raw`, and porcelain's default `--untracked-files=normal` collapses a wholly-untracked directory to one entry ending in `/`. A Scout capture into a new `raw/<category>/` reported as `?? raw/<category>/`, which failed the `.md` extension filter and was dropped, so every file in it went unscanned and the script printed "raw/ has no dirty files — nothing to do" and exited 0 — while the pre-commit guard scanned those same files' staged diff and blocked the commit. Reproduced: one `.md` carrying a US phone number written to a new `raw/_probe/` gave `scanned: 0`, exit 0; with `--untracked-files=all`, `scanned: 1`, one `us-phone` hit, exit 2. Fixed on `nightly/2026-08-30-improvements` (`6858fdd3`), one flag. Not unit-testable without refactoring `REPO_ROOT` out of module scope — `scrub-raw-pii.mjs` resolves it from `import.meta.url` and therefore always operates on the real repository — so the verification is the live before/after above, recorded here in place of a test.
- [x] 2026-08-30 → 2026-08-30 — **The scrubber reported "safe to commit" for files the hook rejects** — its documented exit-code contract is that 0 means "nothing blocking a commit", and its header promises that anything the hook objects to but cannot be auto-redacted "is reported and left alone". `REPORT_ONLY` carried one pattern per category where the hook carries several, and the narrower half was the half that fires on captured third-party prose: no counterpart for bare `\bspouse\b`/`\bwife\b`/`\bhusband\b`, `\bmichelle\b`, the bare `hiring manager` + role-screen phrase, the company+screening co-occurrence, the pay-talk pattern, or three of the five therapy patterns. (Those six are named indirectly here on purpose — see the note at the end of this entry.) Reproduced with a synthetic vendor page: `node scripts/scrub-raw-pii.mjs --json` → `flagged: []`, exit **0**; `bash scripts/hooks/pre-commit` on the same staged file → exit **1** on three separate patterns. After: three triggers and exit 2 — the pay-talk one, `family-relation` and `named-person`. Fixed on `nightly/2026-08-30-improvements` (`c573cf06`). The false-positive cost was measured rather than assumed — `--audit --json` over all **184** files in `raw/` flags the same **2** files before and after, so the nine added patterns cost zero on the real corpus. Carries `tests/scrub-raw-pii.test.mjs` (5 tests, fails 2/5 against the pre-fix list) and the entry-point guard needed to import the module without running its CLI. See the new Open item above for why the *class* is not closed.
  - The note promised above, and it is evidence for that Open item rather than an aside: **the first draft of this entry could not be committed.** Naming two of the missing patterns plainly — the three-word hiring phrase, and a pay word followed within eighty characters by a bargaining word — put them into `docs/NIGHTLY-BACKLOG.md`, which is not on the hook's `WHITELIST_FILES`, and the hook rejected the commit — the 2026-08-28 "a tracked file that cannot be committed leaves the tree permanently dirty" failure, re-created by the act of *documenting* the deny-list. Resolved by rewording rather than by widening the exemption list, since a third exempt file is a worse trade than a paraphrase. Worth knowing for any future entry that quotes a pattern: `\bmichelle\b` and `\bwife\b` written inside backticks are *safe*, because the literal `\b` prefix removes the word boundary the regex needs; a bare multi-word phrase is not.

- [x] 2026-08-25 → 2026-08-29 — **Bus listings sorted malformed items to the top and deferred to the machine's locale** — `listBusItems` (`lib/agent-runtime/bus.mjs`) and `listRepoBusItems` (`lib/repo-runtime/bus.mjs`) both sorted with `String(b.meta.created_at).localeCompare(String(a.meta.created_at))`. Both defects the entry named are reproduced and pinned. (a) An item with no `created_at` stringifies to `"undefined"`, which sorts above every ISO date: a channel holding one undated item and two dated ones listed `['disc-no-date','disc-newer','disc-older']`. This is not only cosmetic — because the sort precedes `slice(0, limit)`, the junk consumed the window: a channel with one `created_at: 'not-a-date'` item and one real item returned `['esc-junk']` at `limit: 1`, hiding the only real item. (b) `localeCompare` is locale-sensitive, the exact call `lib/repo-runtime/queries.mjs:byString` refuses to use with a comment explaining why, so the two bus modules disagreed with the repo's own stated requirement. Replaced with the fix shape the entry specified — `Date.parse` to a number, non-finite last, `id` as tiebreak — as one exported `compareBusItemsByCreatedAt` in `agent-runtime/bus.mjs` that `repo-runtime/bus.mjs` imports, rather than a second copy: the twins already share four imports and this file's own atomicWrite and frontmatter entries are both about copy-pasted helpers. Ordering is now by *instant*, so two equal instants written at different UTC offsets no longer sort as if one were older. Well-formed same-format ISO listings are byte-identical before and after, so blast radius is confined to malformed and tied items. Six regression tests in `tests/agents/bus-ordering.test.mjs`; four verified RED against the pre-change comparator with the call sites reverted and the helper left in place (`['disc-no-date','disc-newer','disc-older']`, `['esc-junk']`, `['std-b','std-a']`, `['rd-no-date','rd-newer','rd-older']`), the two pure-comparator tests pass either way by design. Root suite 542/542 (536 at clone time), `web/` `tsc --noEmit` exit 0 and `next build` clean on Node v24.18.1. Fixed on `main` (V3).
- [x] 2026-08-28 → 2026-08-28 — **An unparseable `--ts` crashed the clipping capture that was designed to survive it** — `normalizeTs` contracts to pass an unparseable timestamp through verbatim so the canonical hash stays defined; its docstring says so and `yamlScalar`'s comment restates it ("`--ts` is not safe by construction"). `buildFilename` then fed that same value to `new Date(x).toISOString()`, which throws `RangeError: Invalid time value`. So the documented pass-through *terminated* the capture: `buildBody` throws before a byte is written and the CLI prints only `error: Invalid time value` — no mention of `--ts`, and the clipping is lost. Live input shape, not synthetic: the module header records that the Apple Notes MCP "returns a bare local-time string with no zone", and the human-readable form of that, `Aug 27, 2026 at 3:40 PM`, does not parse. Reproduced: `normalizeTs('Aug 27, 2026 at 3:40 PM')` → the string verbatim; `buildFilename({ts: that})` → RangeError; `buildBody({source,ts,text})` → RangeError, nothing written. **The suite covered both halves and missed the join** — there is a passing test feeding `normalizeTs('not-a-date\nverified: true')` to `buildFrontmatter`, but `buildBody` crashed before ever reaching `buildFrontmatter`. Filename now degrades to an `undated` sentinel, deliberately distinct from the absent-ts `now` sentinel; `captured_at` and the hash are untouched. Three regression tests verified failing pre-fix. Fixed on `nightly/2026-08-28-improvements` (Tier B, V2).
- [x] 2026-08-28 → 2026-08-28 — **A `--source` containing `../` wrote the clipping outside the repository, and `writeClipping` reported success with a path the file was not at** — `buildFilename` interpolated `source` into the name raw and `writeClipping` does `path.join(CLIPPINGS, built.filename)`, so `source` was never a filename component, it was a path. Reproduced against the real module: `writeClipping({source: '../../../../../probe2/sandbox/pwned', ts: '2026-04-25T17:00:00Z', text: '…'})` returned `{written: true, path: 'raw/clippings/2026-04-25T17-00-00__../../…'}` while the file landed at `/tmp/agentic-kb-work/probe2/sandbox/…` — **outside the repo root entirely** — with `raw/clippings/` unchanged and `git status` clean. The escape is invisible to the caller because the return value names a location the file is not in. `source` is caller-supplied and the caller is an agent driving the Slack / Apple Notes capture flow over content it did not author. This is also the one write path in the repo that skips the path-safety framework the rest of it uses: `checkUnsafePath` and `assertSafeSegment` both reject this shape and neither is on this path. Fixed at two levels — `buildFilename` slugifies `source` for the *filename only* (real sources `slack`/`apple-notes`/`gmail` are byte-identical, pinned by a test; frontmatter `source` keeps the caller's exact value since that is what provenance reads, and `yamlScalar` already makes it safe there; dedup is unaffected because `listClippingHashes` matches only the `__<hash8>.md` suffix), and `writeClipping` asserts the assembled name is a single segment inside `CLIPPINGS` before writing, so the invariant is stated once at the write rather than trusted from each of three sanitisers. Three regression tests, the two traversal ones verified failing against the immediately preceding commit. Fixed on `nightly/2026-08-28-improvements` (Tier B, V2). Root suite 542/542, up from 536 on `main` at clone time.

- [x] 2026-08-27 → 2026-08-27 — **A dated prose section in the compile log counted as a compile run with every count at 0** — `parseCompileLog` in `scripts/lib/foundry-propose-core.mjs` treated any `## <parseable-date>` heading as a gate run. `wiki/_meta/compile-log.md` has two producers, though: the 2-source gate appends `## <iso>` blocks carrying `- promote:` / `- defer:` / `- graduate:` counts, and morning-review appends `## <yyyy-mm-dd>` prose sections that carry none. A prose date parses fine, so those sections became runs with every count at 0 — and both detectors read the list positionally. A prose section as the *newest* heading makes `detectHeavyBacklog` read the latest run's `defer` as 0 and report nothing however large the real backlog is; as the *oldest*, it becomes the `oldestRun` first-seen proxy `detectStuckCandidates` measures every candidate's age against, so the entire stuck detector switches on or off by a prose entry's date. Live on `main` at clone time: three gate blocks plus one `## 2026-08-19` prose section, and `foundry-propose --plan` reported "compile runs scanned: **4**" — it reports 3 after the fix, with the proposal set unchanged. Reproduced with a two-block log (one gate run at `defer: 200`, then a newer dated prose section): `detectHeavyBacklog(runs, {threshold: 50})` returned `[]`. A block is now a run only if it states at least one count. Three regression tests in `tests/foundry-propose.test.mjs`, all verified failing pre-fix (28/31) and passing after (31/31); full suite 539/539. Fixed on `nightly/2026-08-27-improvements` (Tier B, V2) — behaviour-changing, so not on `main`.
- [x] 2026-08-25 → 2026-08-26 — **The predicted Next.js security release landed, and it is worse than the one CRITICAL that was forecast — it is two** — the 08-25 entry said to wait for the advisory rather than bump speculatively. Correct call: the release is **16.3.3**, published 2026-08-25T15:32Z, and it is a pure security release whose notes contain nothing but two CRITICAL advisories and no other change — `GHSA-p293-qw3h-jr36` (unauthenticated RCE on windows-hosted servers) and `GHSA-2xp9-vwfh-vxw4` (unauthenticated RCE in the Image Optimization API when AVIF files are used). `web/` resolved `next` to 16.3.1, on the affected line and predating both. Bumped `^16.2.6` → `^16.3.3` on `nightly/2026-08-26-improvements` (Tier B). Verified on Node v22.14.0: `npm ci --include=dev` clean, `npx tsc --noEmit` exit 0, `npm run build` exit 0, run both before and after. Three things worth carrying forward. **(1) `npm audit` is blind to both** — it reported "found 0 vulnerabilities" before *and* after the bump, because the advisory database has not ingested either GHSA; `api.github.com/advisories/<id>` and OSV both 404 them as of this run. A clean audit was not evidence and must not be treated as one for at least the next few nights. **(2) Reachability could not be established, only bounded** — neither advisory body is readable yet, so severity and ranges here come from the release notes, not a parsed advisory record. This app imports `next/image` nowhere and sets no `images` key in `next.config.ts`, but `/_next/image` is mounted by `next start` regardless, so the AVIF one is not ruled out; the Windows one does not apply to the macOS host but does apply to any deployment. **(3) The nightly research manifest got this repo wrong** — it stated "Next.js August 2026 critical advisory does not apply here (no Next dependency)", which is false: `web/package.json` has depended on `next` since the initial scaffold. Only the *root* `package.json` lacks it. A manifest sweep that reads one manifest per repo will keep missing `web/`, `mcp/` and `lib/agent-runtime/`.

- [x] 2026-08-25 → 2026-08-25 — **The bus TTL sweep never saw the items it exists to archive** — `runBusTTL` asked `listBusItems` for `{ limit: 1000 }`. `listBusItems` parses every file in the channel and *then* sorts newest-first and *then* truncates, so the limit bought no work — it only discarded results, and it discarded exactly the wrong ones. On a channel holding more than 1000 items the sweep was handed the 1000 *newest*, which are by definition the items not yet past TTL, and never examined the oldest ones at all. The failure is silent: it returns `{ archived: [] }`, which reads identically to "nothing was due", so a `discovery` channel that crosses 1000 items stops ageing out entirely and nothing reports it. Reproduced with one item backdated 400 days plus 1200 dated today — `listBusItems(limit:1000)` returned 1000 rows without the ancient one, and the 400-day-old item was still on the bus after the sweep. Regression test in `tests/agents/retention.test.mjs`, verified failing pre-fix. Fixed on `main` (V3).
- [x] 2026-08-25 → 2026-08-25 — **Namespaced candidates were invisible to the TTL sweep** — the theme slug class in `scripts/candidates-ttl.mjs` was `[a-z0-9][a-z0-9-]*`, with no `/`, but `compile-2source-gate.mjs:113` writes each theme straight from a wikilink / `key_concepts` target and those are routinely namespaced (`concepts/llm-wiki`, `patterns/pattern-fan-out-worker`). The capture stopped at `concepts`, `\(` then had to match `/`, and the line was skipped. Those candidates were never tracked, never aged, never expired, and were not counted in the `Candidates: N total` line — and because the rewrite filter used a second copy of the same class, they also survived every rewrite, which is why they accumulated. Reproduced on the live vault: `wiki/candidates.md` holds 164 candidate bullets, 10 namespaced, and the script reported `Candidates: 154 total`; it now reports 164. Both regexes now share one `THEME_SLUG` constant, because a parser/filter disagreement archives a theme while its line survives and resurrects it next run. `writeUniqueArchive` now creates the parent directory so a namespaced name archives into a matching subdirectory instead of throwing `ENOENT` — a latent crash the old regex was hiding. Regression test in `tests/candidates-ttl.test.mjs`, verified failing pre-fix. Fixed on `main` (V3).
- [x] 2026-08-25 → 2026-08-25 — **The `[UNVERIFIED]` trust penalty had never once applied** — `scoreTrust` gated half its verification bonus on `fm.body.includes('[UNVERIFIED]')`, but `fm` is the frontmatter *data map*; the document text is `parsed.content`, which `readFrontmatterCached` parsed and threw away. `fm.body` was therefore `undefined` for every real page and the branch was dead code. That branch was the one that mattered: `[UNVERIFIED]` in this vault is an inline body marker, not a tag — `wiki/system/policies/source-trust-policy.md` defines the 0.80 multiplier, 34 pages carry the marker in their text, and **zero** carry it in `tags`. So an explicitly unverified claim scored identically to a sourced one, and `promotion-scorer` consumed that score as evidence quality. Reproduced: a `confidence: high` page whose body reads "[UNVERIFIED] no source measures this" scored 0.95 / `trusted`, the same as an unmarked page; it now scores 0.76 / `reliable`. Only the boolean is cached, not the body — the cache is keyed by path with no eviction. Two regression tests in `tests/agents/source-trust.test.mjs`; the body test verified failing pre-fix, the tags test passes both ways to pin the branch that was already live. Fixed on `main` (V3).
- [x] 2026-08-20 → 2026-08-23 — **The stacked unmerged nightly branches** — `nightly/2026-08-20-improvements`, `-08-21-` and `-08-22-` were all drained into `main` on 2026-08-23 via `425fb7cd`, `7b307362` and `dd457bfb`. Verified on 2026-08-24 by `git merge-base --is-ancestor` on all twelve branch commits (all ancestors of `main`) and `git ls-remote --heads origin 'nightly/*'` (empty — branches deleted). This closes both the 2026-08-22 three-branch entry and the 2026-08-21 "merge or close the 08-20 branch" entry, and with them the red suite they were holding the fix for: `npm test` on `main` at 2026-08-24 clone time is **532 pass / 0 fail**, against 514 pass / 1 fail two nights earlier. Everything those branches gated is now unblocked.
- [x] 2026-08-21 → 2026-08-24 — **`confidence`, `related_sources` and `evidence_count` are now carried onto the promoted page** — `promoteDiscovery` read all three off the bus item to build the scorer candidate and then dropped them when serializing, so the V2 provenance block recorded how a promotion was decided but not what it was decided about. Reproduced by promoting two candidates through the same gate — `confidence: high` / `evidence_count: 9` / three cited sources, and `confidence: low` / `evidence_count: 1` / none. Both cleared as `learned` (0.702 and 0.566) and produced frontmatter identical on every qualifier, so an uncertain observation read as unqualified fact and its citations were reachable only by following `source_path` back to the bus item. Defaults now match the ones `scorePromotion` applies (`medium` / `[]` / `0`), so the page records what was scored and the unscored (`skipScorer`) path states the same fields rather than omitting them. Three regression tests in `tests/agents/promotion-provenance.test.mjs`, all three verified failing against the pre-fix source. Fixed on `nightly/2026-08-24-improvements`.
- [x] 2026-08-22 → 2026-08-22 — **A damaged `config/repos/registry.json` read as "no repos", and the next write erased them** — `loadRegistry` did `catch { return [] }`, so a truncated or otherwise unparseable registry was indistinguishable from an empty one. That empty list fed `upsertRepo`, which pushed one record and called `saveRegistry`, which wrote a one-repo registry over the damaged file — losing every other record *and* the evidence, at exit status 0. `saveRegistry`'s own comment had named this exact failure and fixed only the write half with tmp+rename. Reproduced by truncating a three-repo registry to half its bytes: `listRepos` → `[]`, one `upsertRepo` → a registry containing only the new repo. Now throws on a file that exists and cannot be understood; a missing file and a hand-written `{ "version": "1.0" }` wrapper are still legitimately empty. Fixed on `nightly/2026-08-22-improvements`.
- [x] 2026-08-22 → 2026-08-22 — **A knowledge gap in the lint ledger could never be closed, only accumulated** — `reconcileFindings` in `lib/wiki-lint.mjs` clears a finding when a run re-examined every page it references. The guard `refs.length > 0 && refs.every(...)` is correct for contradictions but false for *every* gap on *every* run, because a gap has no `pages` — so no run could ever resolve one, and the comment directly above the guard promised the opposite. The ledger is persisted to `logs/lint-state.json` and re-rendered each morning, so a gap somebody had since filled stayed in the report permanently. Reproduced: one gap, eleven consecutive full-vault runs, `resolved.length` 0 every time, while a contradiction over the same set cleared on the first. Gaps now age out over one full rotation of the analysis window (the lint route passes the rotation length it already computes); a run that examined nothing does not advance the counter and a re-report restarts it. Contradiction handling unchanged. Fixed on `nightly/2026-08-22-improvements`.
- [x] 2026-08-21 → 2026-08-21 — **`archiveMove` overwrote an existing archived record** — found and fixed in one run, recorded here so a future run reading `retention.mjs` does not re-derive it. The "archive never delete" module used a plain `copyFileSync` into a flat namespace keyed by the caller-supplied `task_id`; `startTask`'s exclusive-create guard only covers the *live* working-memory file, so reusing an id after retention archived it destroyed the first task's only copy. Fixed on `nightly/2026-08-21-improvements` with `COPYFILE_EXCL` + bounded `-2/-3…` suffixes, matching `exclusiveSnapshotWrite` two functions above; `archiveMove` now returns the path actually written and all three call sites report it.
- [x] 2026-08-21 → 2026-08-21 — **`resolveRepoWikiDir` returned the caller's casing on a case-insensitive filesystem** — its `fs.existsSync` fast path is case-insensitive on APFS and NTFS, so the canonicalising readdir fallback below it was dead code on every Mac and Windows checkout, and `tests/repos/repo-queries.test.mjs` failed there (512 pass / 1 fail on `main` at 2026-08-21 clone time). Now resolved against the directory listing. Fixed on `nightly/2026-08-21-improvements`.

## Checked, not applicable

- 2026-09-01 — **The brief said `next == 16.3.1` is CLEAN. It is not, and this is the fourth consecutive night the brief has been wrong about `web/`.** Re-derived rather than trusted, as the 2026-08-30 entry below instructs. `web/package-lock.json` on `main` resolves `next == 16.3.1`; the brief reasoned from the **July 2026** batch (floor 16.2.11) and concluded clean. There is a separate **August 2026** release, disclosed 2026-08-25, fixing two critical unauthenticated RCEs, and its floor is **16.3.3**: CVE-2026-75604 (Windows path traversal, affects 13.4–15.5.24 and 16.0–16.3.3) and GHSA-2xp9-vwfh-vxw4 (RCE via libheif under `sharp` when optimizing an attacker-controlled AVIF, affects 10.0.0–15.5.24 and everything below 16.3.3). **16.3.1 is below the floor for both.** So the brief checked the wrong advisory batch and inverted a live CRITICAL for the second time in three nights — the mirror image of the 2026-08-30 error, which claimed the repo had no Next.js at all.
  - **Exposure here is configuration-limited but non-zero, and this is the honest version of the severity.** `web/next.config.ts` declares no `images` block, so `remotePatterns` is empty and `/_next/image` will not fetch a remote URL; and there are no `next/image` imports under `web/src`. The Windows CVE does not apply (this deploys on macOS/Linux). What remains is that the optimizer endpoint still exists and still serves local paths, and the app is a KB that ingests `raw/` content. That is a reduced attack surface, not an argument for staying on a vulnerable build.
  - **No bump was written tonight, deliberately** — the exact fix already exists and is green on `nightly/2026-08-26-improvements` (`497ebcca fix(web): bump next 16.3.1 -> 16.3.3`), which was verified this run to still merge cleanly into current `main`. Re-deriving it would produce a second commit to reconcile, not faster remediation. Escalated instead: a PR was opened for that branch and it is ranked first in the recommended merge order.
  - Also re-derived from `web/package-lock.json` and confirmed **clean**, so future runs can stop re-checking: `sharp == 0.35.3` (≥ 0.35.0 floor for GHSA-f88m-g3jw-g9cj), `form-data == 4.0.6` (= floor for GHSA-hmw2-7cc7-3qxx), `react`/`react-dom == 19.2.4`, `gray-matter == 4.0.3`, `@anthropic-ai/sdk == 0.32.1`. Root, `mcp/` and `cli/` declare no advisory-relevant hot dependencies.

- 2026-09-01 — **All eight pre-existing nightly branches merge cleanly into current `main`; the brief's "all 8 are `diverged`" is a status word, not a conflict prediction.** The brief presented the eight as `diverged` "unlike other repos where they merge cleanly", which reads as a warning that they conflict. `diverged` in the GitHub compare API only means both refs have unique commits — which is trivially true of any branch that has not been rebased. Measured with `git merge-tree --write-tree main <branch>` for all eight: **zero conflicts**. Separately, none is superseded: `git log --cherry-pick --right-only main...<branch>` returns a non-empty commit list for every one of the eight, so no branch can be closed without merging on supersession grounds.

- 2026-09-01 — **The `NODE_ENV=production` devDependency trap does not apply to this repo's root suite.** The brief warns that an inherited `NODE_ENV=production` makes `npm ci` drop devDependencies and produce bogus missing-`vitest`/missing-eslint errors. Checked: root `package.json` declares **no `devDependencies` at all** (two runtime deps, `chokidar` and `yaml`), and both are committed under a force-added `node_modules/` (247 tracked files), so the root suite needs no install and `npm test` is `node --test` over three globs. The trap is real only for `web/`, which does carry devDependencies (`eslint`, `typescript`, `@types/*`). Recorded so a future run does not spend time on a red baseline that cannot occur here. Every command this run was still prefixed with `unset NODE_ENV`.

- 2026-08-31 — **The `next` 16.3.1 → 16.3.3 bump was NOT re-attempted, deliberately.** Verified from the tracked manifests that `main` is still on 16.3.1 (see the branch-count entry above), then verified that `nightly/2026-08-26-improvements` already carries the exact fix (`497ebcc fix(web): bump next 16.3.1 -> 16.3.3 for two critical unauthenticated RCEs`). Re-deriving a fix that already exists on an unmerged branch produces a second commit to reconcile, not a faster remediation. Escalated in the report instead.

- 2026-08-31 — **No `workflow-patch-*.diff` was written this run, because an identical one already exists.** The 2026-08-27 CI-coverage entry records that the CI glob fix and the Node-20-EOL `.nvmrc`/`node-version` bump were **re-issued as one combined patch** on 2026-08-30 precisely because splitting them had cost six nights. A third copy of the same diff in a third night's outputs is the failure mode that entry is about. What tonight adds instead is that `CONTRIBUTING.md` no longer *claims* CI covers the suite (`12269d51`) — the false claim was the part a nightly can actually fix without `workflow` scope. Current numbers for whoever applies the patch: 571 tests total, 334 in the `tests/agents/` glob CI runs, **237 that CI has never run**.

- 2026-08-31 — **`lib/search/embeddings.mjs` has zero tests and will keep having zero tests.** It is a documented stub: `NOT_ACTIVATED = true` and a `makeProvider` whose entire body is `throw new Error('embeddings: not activated')`. A test asserting that a two-line throw throws is the padding this backlog exists to prevent. Revisit when the header's activation trigger fires (wiki ≥ 500 pages or keyword recall < 60%).

- 2026-08-31 — **`web/src/lib/safe-path.ts` and `lib/agent-runtime/safe-path.mjs` are logically identical — the only drift is a docstring, and it is the TypeScript copy that is wrong.** Diffed after type-stripping: the two `safeJoin` bodies and the two `validateSlug` bodies are byte-identical. The `.ts` copy documents `validateSlug` as "lowercase, digits, hyphen, slash, underscore" while the regex it sits above is `^[a-zA-Z0-9][a-zA-Z0-9/_.-]*$` — uppercase and `.` are both accepted. Not corrected tonight only because it is a one-line docstring in a file no other change touched; noted so the next run can fold it into any `web/` commit rather than spending a commit on it. The new `tests/agents/safe-path.test.mjs` pins the real charset, so the code side is now guarded either way.

- 2026-08-31 — **`lib/agent-runtime/identity.mjs` and `web/src/lib/rbac.ts` both export `resolveIdentity` and are not twins — checked for divergence, found none that matters.** Same name, different jobs: the runtime one produces an attribution record (`{id, kind, namespace, source}`) and is never consulted for access, the `rbac.ts` one resolves a namespace + ACL from `namespaces.json` and *is* the authorization decision. The only real divergence was the Bearer parsing, and it was the runtime copy that was wrong (fixed tonight, see Closed). `rbac.ts`'s own guards hold up under an input-space read: `Object.hasOwn` on both the token and namespace lookups (no `constructor`/`toString` hit), the `X-KB-Namespace` header honoured only while `cfg.tokens` is empty, and `loadConfig` backfilling `tokens`/`namespaces`/`default` after `JSON.parse`. The shared name is still a trap for a reader; renaming it is a public-interface change and not a nightly's call.

- 2026-08-30 — **The research manifest was wrong about this repo's manifests for a third night, and this time it inverted a live CRITICAL.** Tonight's brief stated "Next.js August 2026 security release (15.5.24 / 16.3.3) — **not applicable, this repo has no Next.js**". `web/package.json` pins `"next": "^16.2.6"` and `web/package-lock.json` on `main` resolves it at **16.3.1**. So the item is not merely applicable, it is the single highest-severity thing in this backlog: 16.3.3 fixes two critical unauthenticated RCEs, the bump exists and is green on `nightly/2026-08-26-improvements`, and `main` has been on 16.3.1 for four nights. Recorded here rather than as a new Open item because it is already Open (see the branch-count entry) — what is new is that **the brief's negative findings cannot be trusted as a reason to skip a check**. The 08-28 entry below made the same point about three manifest errors; a third night makes it a property of the input, not an accident. Concretely: re-derive from the tracked manifests, and treat "not applicable" in a brief as a hypothesis.
- 2026-08-30 — **MCP spec `2026-07-28` — not re-derived, fourth time it has been raised.** The 2026-08-20, 08-24 and 08-27 entries below settle it: `mcp/server.js` instantiates `StdioServerTransport` and nothing else, so the stateless-core / session-header changes have no surface here. Nothing was read tonight beyond confirming that line is unchanged. This item should be dropped from the research rotation.
- 2026-08-30 — **`lib/agent-runtime/source-trust.mjs` — a genuine `Object.prototype`-key hole, ruled below the bar for exactly the reason `generate-stats.mjs` was on 08-29.** `CONFIDENCE_MULTIPLIERS[confField] ?? CONFIDENCE_MULTIPLIERS.unverified` (line 87) keys a plain object literal with `(fm.confidence || '').toLowerCase()`, straight off untrusted page frontmatter. `??` only falls through on `null`/`undefined`, and `CONFIDENCE_MULTIPLIERS['constructor']` is the `Object` constructor — truthy — so `confidence: constructor` in any page makes `confidenceMultiplier` a *function*, `rawScore` `NaN`, and `scoreTrust` return `{ score: NaN, label: 'unverified' }`, which then feeds context-loader ranking. `resolveConfidence` (line 143) has the identical shape. Not patched: this is the same near-miss class as `generate-stats.mjs`, `confidence:` in this vault is the closed vocabulary `high|medium|low|unverified`, and the 08-29 sweep's own reasoning — the key must be *plausible*, not merely *possible* — applies unchanged. Recorded so the next sweep does not re-derive it, and so that if `confidence:` ever becomes free-text this flips to live. The `Object.create(null)` fix is one line if it does.
- 2026-08-30 — **`stampFrontmatter`'s `String.replace` treats `$&` in existing frontmatter as a substitution pattern — left alone under the frontmatter freeze.** `scripts/scrub-raw-pii.mjs:~200` does `content.replace(fmMatch[0], template)` with a *string* pattern, so `$&`, `` $` ``, `$'` and `$$` in the replacement's interpolated `body` are expanded rather than copied. Probe: frontmatter `title: Deal $&now` yields `---\ntitle: Deal ---\ntitle: Deal $&now\n---\nnow\npii_redacted: true\n---` — the entire matched block spliced into the title, producing a nested `---` and an unparseable file, written in place on `--execute`. Real, and only reachable via `--execute` on a page whose frontmatter contains one of those four sequences (`$n` is *not* expanded for a string pattern, which removes the most plausible trigger). Not fixed: `stampFrontmatter` is one of the five hand-rolled frontmatter codecs enumerated in the 2026-08-20 Open entry, the codec is under the whack-a-mole freeze at five prior same-class fixes, and a sixth patch here is exactly what that rule exists to prevent. The one-line form is `replace(fmMatch[0], () => template)`; it belongs in the consolidation, not before it.
- 2026-08-30 — **`dirtyRawFiles` does not un-escape git's quoted paths.** With `core.quotepath` at its default, a path containing non-ASCII or control bytes is reported as `"raw/caf\303\251.md"`; the code strips the surrounding quotes but leaves the octal escapes, so `fs.readFile` throws `ENOENT` and `main()`'s catch exits 1 — the scrubber crashes instead of clearing the file. Left alone: `raw/` is 184 files and every tracked path is ASCII (checked tonight: `git ls-files raw` piped through a Python `any(ord(c) > 127 ...)` filter → 0 of 189; note BSD `grep` has no `-P`, so the obvious one-liner does not run on the nightly host), and Scout's capture filenames are slugified, so there is no producer that can create one. Fix if that changes: add `-z` to the `git status` call and split on NUL, which removes quoting entirely rather than un-escaping it.
- 2026-08-30 — **Three more low-churn modules read end to end with nothing found; diff against this rather than re-reading them.** Picked for 0–2 prior nightly commits and no coverage in the 08-27 or 08-29 sweeps. (a) `lib/repo-runtime/queries.mjs` (252 lines) — every entry point routes through `resolveRepoWikiDir`, which validates via `repoWikiRoot` *before* `readdir` and only ever returns a name that came from the directory listing; `listRepoDocs` and `readRepoCanonical` both `safeJoin` caller input before any read; `walkMarkdown` uses `isFile()` so a `.md` symlink out of the vault is not followed; sorting is `byString`, not `localeCompare`. One cosmetic nit, not worth a commit: the exact-match branch omits the `isDirectory()` test its case-insensitive sibling has, so a *file* named exactly like the repo would be returned as the wiki dir — every consumer then `readdir`s it, fails, and returns `[]`. (b) `lib/agent-runtime/identity.mjs` (104 lines) — the `Bearer` path fingerprints with SHA-256 and never puts raw token bytes in the `from` field or the audit log, with a comment saying why; the header-dict fallback is a correct case-insensitive lookup. (c) `lib/agent-runtime/hot-learned.mjs` (112 lines) — the `wx` exclusive-create loop with a bounded suffix retry correctly handles two processes closing a task in the same second, and the loss report (`source_lines`/`digest_lines`/`lines_dropped`) is computed outside the summarizer so a registered custom summarizer is measured identically.

- 2026-08-29 — **The 2026-08-04 npm supply-chain compromise ("Keyv / Cacheable", self-propagating, Shai-Hulud-shaped) — chokidar is not affected here, and the committed dependency trees are clean.** Checked properly rather than bumped blind, because the research brief named chokidar specifically and this repo pins `chokidar ^5.0.0`. (a) **Version window.** `package-lock.json` resolves chokidar to **5.0.0**, `npm view chokidar time` dates 5.0.0 to **2025-11-25**, and 5.0.0 is still the newest published version — npm shows *no* chokidar release on or after 2026-08-04, so there is no compromised version to have been pulled and nothing to roll back to. `readdirp` is 5.0.0, `yaml` is 2.8.3. `web/` carries chokidar **3.6.0** transitively (2024-02-06); `mcp/` has none. All predate the window by 8+ months.
  - (b) **`npm audit` is clean in all three manifests**: root `found 0 vulnerabilities` (4 packages), `mcp/` `found 0 vulnerabilities` (94 packages), and the `web/` result is unchanged from the 2026-08-26 entry. No high or critical to triage.
  - (c) **The committed trees were verified byte-for-byte, not just by version string** — this is the check that actually addresses a self-propagating payload, since a worm modifies files in place and leaves the version untouched. `diff -rq` of the committed `mcp/node_modules` against a clean `npm ci --ignore-scripts` reported **zero content differences across all 3,465 shared files**, and zero files present only in the committed tree. Whatever else is wrong with that tree (see the new Open item above — it is missing 19 files), nothing in it has been altered. The 247 tracked files under root `node_modules/` (chokidar, readdirp, yaml) match their lockfile versions likewise.
  - Recorded at this length so a future run diffs against it instead of re-deriving it. Re-check only if a chokidar release appears dated on or after 2026-08-04.

- 2026-08-29 — **Five zero-or-low-churn runtime modules read end to end with one finding; diff against this rather than re-reading them.** Picked because they carry 0–1 prior nightly commits. (a) `lib/agent-runtime/state-machines.mjs` — sound, and already hardened: both `canTransition` and `transition` use `Object.hasOwn` against crafted `status` values from frontmatter, with a comment saying why, and an unknown `from` yields `[]` so the whole thing is fail-closed. (b) `lib/agent-runtime/path-safety.mjs` — re-confirmed complete; it is what blocks the otherwise-unvalidated `rw.type` / `rw.project` interpolation in `_planWrites`'s rewrite path (`writeback.mjs:90`), which would otherwise be a traversal. (c) `lib/repo-runtime/metadata.mjs` and `lib/search/embeddings.mjs` — nothing to find; the latter is a documented stub that throws. (d) `lib/agent-runtime/scaffold.mjs` — sound; `id`, `domain` and `team` are all charset-validated before interpolation into contract YAML and wiki paths. (e) `lib/agent-runtime/writeback.mjs` (555 lines, **zero** prior nightly commits, the central transactional write path) — traced the full plan/guard/commit/rollback cycle including the bus-publish rollback, which was the suspected fail-open: `publishBusItem` returns a *kbRoot-relative* path, so `path.join(kbRoot, step.path)` in `rollbackWrites` resolves correctly and a rolled-back transaction does delete its bus items. No finding.

- 2026-08-29 — **The same Object.prototype-key class swept across the rest of the codebase; only `candidates-ttl.mjs` was live.** After fixing that one (see the nightly branch), every other dynamic-key dictionary was checked. Not reachable: `ingest-dedup.mjs:140` and `clipping-write.mjs` key by sha256 hex, which cannot spell an inherited key; `lib/wiki-lint.mjs` uses a real `Map`; `contracts.mjs:124` parses trusted config and the production path is the real `yaml` package, not `parseYamlLite`. One cosmetic near-miss, deliberately not patched: `scripts/generate-stats.mjs:130` `typeCounts[type] = (typeCounts[type] || 0) + 1` would render a page with `type: constructor` as the string `"function Object() { [native code] }1"` in the stats page — but unlike the candidates slug class, `type:` values in this wiki are a small closed vocabulary (`meta`, `archive`, `decision`, `concept`, `pattern`) and `constructor` is not a plausible member. Note the asymmetry that made candidates-ttl the real one: assignment to an inherited *writable data* property does create an own property, so `typeCounts` self-heals into a visible garbage entry, whereas candidates-ttl's `if (!tracker[k]) tracker[k] = {...}` guard means the assignment never happens at all and the key stays invisible forever.

- 2026-08-29 — **`--stale-days` / `--top` / `KB_CANDIDATES_TTL_DAYS` NaN handling — checked, all fail safely, no change.** The flag reader in `scripts/static-lint.mjs:36` returns `argv[i+1]` unguarded, so a trailing `--stale-days` yields `parseInt(undefined)` → NaN. Traced through: `isStalePage` (`lib/wiki-lint.mjs:116`) already has an explicit `!Number.isFinite(staleAfterDays) → return false` guard, so the result is a report with zero stale pages rather than a crash or a wrong number, and `--top NaN` slices to an empty list. `candidates-ttl.mjs:27` with a malformed env var is better still: `new Date(NaN).toISOString()` throws `RangeError: Invalid time value` and the run dies loudly. Both require deliberate flag misuse by a human at the CLI — neither cron caller passes these — so the guard is adequate and tightening it would be churn.

- 2026-08-28 — **The research manifest was wrong about this repo's manifests for the second night running — three separate items.** Recorded together because the pattern matters more than any one of them, and it is the same failure the 2026-08-25 Next.js entry already named: *a sweep that reads one manifest per repo keeps missing `web/`, `mcp/` and the lockfiles.* (a) **"There is no root `package-lock.json`"** — there is. `git ls-files package-lock.json` returns it; it is `lockfileVersion: 3` and pins `chokidar` 5.0.0, `readdirp` 5.0.0 and `yaml` 2.8.3 with integrity hashes. The follow-on premise — "floating `^` ranges mean every install resolves fresh" — is therefore false for `npm ci`, which is what every job here runs. Nothing to file; no supply-chain finding exists. (b) **"No `sharp` dependency, so the libheif/AVIF advisory does not apply"** — `sharp@0.35.3` *is* in `web/package-lock.json`, as an `optionalDependencies` entry of `next` (`node_modules/sharp`, `"optional": true`), which is exactly how Next ships Image Optimization. It is not a root dep, which is presumably what the sweep looked at. Whether 0.35.3 is in the affected range is **not established here** and should not be assumed either way: `api.osv.dev` returns **0 vulns for `sharp@0.35.3`** — but it also returns **0 vulns for `next@16.3.1`**, which this file already established on 2026-08-25 is affected by two CRITICAL advisories. So OSV is still blind three nights later and a clean OSV result remains not-evidence, exactly as the 08-25 entry warned. This one deserves a real look by a run that can install `web/`. (c) **"ESLint 9 EOL — no eslint dep at root"** — true at root, but `web/devDependencies` has `eslint@^9.39.5` and `eslint-config-next@^16.2.2`. Moot in practice rather than not-applicable: per the Open 2026-08-22 item, `next lint` was removed in Next 16 and there is no `eslint.config.*` anywhere, so both packages are wired to nothing and no ESLint version runs on this codebase at all.
- 2026-08-28 — **Node 20 EOL — re-checked, nothing new, do not re-derive.** Still Open above and unchanged: `.nvmrc` on `main` says `20`, the `24` commit still sits unmerged on `nightly/2026-08-25-improvements`, and the workflow half is still the one-line patch waiting on Jay (written 2026-08-25, and a separate three-step CI-coverage patch written 2026-08-27). Seven nights Open on that one blocker. **No new workflow patch was generated tonight** — regenerating an identical diff is noise; reference the two that exist.
- 2026-08-28 — **`lib/repo-runtime/templates.mjs` — the two known defects are in genuinely unreachable code, which downgrades them.** The 2026-08-25 entry filed `repoRecord.description` being rendered as the repository *URL* at lines 514 and 532 and left it because the file has zero tests. Re-checked with a call-graph sweep: `generateHomePage`, `generateProgressPage`, `generateRepoCLAUDE` and `generateCanonicalTemplate` have **no callers anywhere** in `lib`, `scripts`, `cli`, `mcp` or `web/src` — the only references are the definitions themselves and the `index.d.ts` declarations. They are re-exported from `lib/repo-runtime/index.mjs` (so they are public API) but nothing in this repo invokes them, which means no generated page carries the wrong-field link today. Still worth fixing whenever the file grows a consumer; not worth a fixture tonight, and a nightly should not spend its budget adding coverage to 712 lines of unreachable template strings. Recorded so a future run does not re-derive the call graph.

- 2026-08-27 — **The 247 tracked files under root `node_modules/` — re-derived from the *full* history and the 2026-08-22 ruling below is confirmed exactly.** Recorded because the research manifest raised it again as an open question ("deliberate vendoring for an offline MCP runtime, or accidental?"). It is accidental. `git fetch --unshallow` then `git log --diff-filter=A` on `node_modules/yaml/package.json` and `node_modules/chokidar/package.json` names `6c4022f9` and `3c00fcd2`, both authored by `jaydubya818` on 2026-04-25 with the message `vault backup: <timestamp>` — the Obsidian-git backup sweep, not a vendoring decision. (Note this is exactly the case the shallow-clone caveat warns about: on a depth-20 clone `--diff-filter=A` attributes them to the oldest commit in the window.) Nothing depends on them: the only `node_modules` references outside `mcp/` and `web/` are two *exclusion* filters, `scripts/ingest-omm.sh:26` (`find … -name node_modules -prune`) and `scripts/autolink.py:120` (a skip-dirs default). `git status --porcelain` is empty after `npm ci`, so the worktree still comes back clean. **Contrast `mcp/node_modules`, which is deliberate** (see the 2026-08-20 entry) — a stdio MCP server launched by Claude Desktop out of a bare clone has no install step. The root tree has no such consumer. No change made: the latent risk is unchanged (the first `chokidar` or `yaml` bump leaves 247 stale tracked paths dirty and trips the night jobs' clean-worktree gate), and `git rm -r --cached node_modules` is still Tier B work for someone who can also see what the vault-backup job will do next.
- 2026-08-27 — **MCP spec 2026-07-28 — third check, same answer, and this is the last time it should need deriving.** `mcp/server.js` instantiates `StdioServerTransport` and nothing else; it declares no protocol version because a stdio server has no handshake of its own — the SDK owns it. The stateless-core and OAuth/OIDC halves of the new spec are both transport-layer concerns for HTTP servers. The versioned Apps and Tasks extensions are opt-in capabilities this server does not advertise. Nothing to change, and nothing here is version-sensitive; a future manifest sweep can skip this repo for MCP-spec items unless `mcp/server.js` grows an HTTP transport. (Two prior identical rulings: 2026-08-24 and 2026-08-20 below.)
- 2026-08-27 — **Five modules audited end to end with nothing found — diff against this instead of re-reading them.** All five have zero or one prior nightly commit, which is why they were picked. (a) `lib/agent-runtime/path-safety.mjs` — the 13-predicate `UNSAFE_CHECKS` table is complete for its stated job and is the single source both write guards share; the fuzzer covers it. (b) `lib/agent-runtime/safe-path.mjs` — `safeJoin` resolves then verifies containment with the `root + path.sep` guard (so `/rootevil` cannot pass as `/root`), and rejects non-string, null-byte and absolute segments first; `validateSlug` requires an alphanumeric first character and rejects `..` explicitly. (c) `lib/agent-runtime/state-machines.mjs` — `canTransition` and `transition` both use `Object.hasOwn`, so a status read from untrusted frontmatter cannot resolve to a prototype member; all three machines have `archived`/terminal sinks with empty transition lists. (d) `lib/agent-runtime/locks.mjs` — liveness before age, `rename`-to-tomb rather than bare `unlink` when stealing, per-attempt `ts` stamping, deadline checked on the cleared path, and release only when the record is still ours. The one thing worth knowing: `lockPath` collapses unsafe characters to `_`, so `a/b` and `a_b` share a lock file — over-locking, which is the safe direction. (e) `lib/agent-runtime/scaffold.mjs` — `id`, `tier`, `domain` and `team` are all charset-validated before interpolation into contract YAML and wiki path segments, with a comment explaining the injection this prevents.
- 2026-08-27 — **`lib/agent-runtime/adr-emitter.mjs` emits `status`, `author` and `confidence` as *unquoted* YAML scalars — left alone under the frontmatter freeze.** Only `title` goes through `JSON.stringify`; the other three are passed through `oneLine()`, which strips CR/LF and therefore does block the key-injection this file's own comment is about, but does not quote a value containing `:`, `#`, `[` or `{`. That is a frontmatter-serialisation defect, i.e. the same class as the Open codec item, and the correct fix is to route this through `serializeFrontmatter` rather than to add a fourth escaping rule here. No patch attempted. Also checked and fine: `nextAdrNumber` uses `parseInt(m[1], 10)` with a radix, and the per-batch `nextN++` cannot collide even when two decisions slugify identically.
- 2026-08-26 — **The `assertWriteAllowed` path-safety chain — audited end to end and found sound.** Recorded because it is the repo's central write chokepoint and a future run should diff against this rather than re-derive it. Checked, all negative: (a) `assertSafeSegment` in `repo-runtime/paths.mjs` rejects a backslash in a repo name via `checkUnsafePath`, so the `isImportedDoc` normalisation (`relPath.replace(/\\/g,'/')`) cannot be used to smuggle a second path segment past `^wiki/repos/[^/]+/repo-docs/` — the exact bypass shape the function's own comment records for `/`. (b) `payload.rewrites[].project` is interpolated into a write path in **both** writeback twins with no validation, but `checkUnsafePath` runs first in `assertWriteAllowed` and rejects `..`, `//` and a leading `/`, so every traversal attempt is fail-closed; a `/` inside the value only nests a directory deeper inside the already-allowed subtree. (c) `planRepoWrites`' repo-scoped fallback (`assertRepoScopedWriteAllowed`) is entered only on reasons `not in allowed_writes` / `no allowed_writes configured`, and `assertWriteAllowed` evaluates unsafe-path and `forbidden_paths` *before* either of those, so neither rejection can be downgraded by the fallback — the comment claiming this is accurate. (d) `SAFE_AGENT_ID` in `contracts.mjs` permits `.` after the first character but requires an alphanumeric first character, so no value can form a `..` segment, and separators are excluded — `loadContract` cannot be steered outside `config/agents/`. The two defects that *were* found in this chain are in `lib/agent-runtime/paths.mjs` and are filed under Open above; they are variable-substitution defects, not path-traversal ones.
- 2026-08-26 — **`dryRunCloseRepoTask`'s `allowed` count — looks like the bug `2a0801df` fixed in the agent-runtime twin, and is not.** The agent-runtime `dryRunCloseTask` carries an explicit comment warning that `planned.length - rejected.length` undercounts because `rejected` also holds vault fan-out rejections. `lib/repo-runtime/writeback.mjs` still computes `allowed: planned.length - rejected.length`. But the repo twin has no vault fan-out and builds `rejected` as `planned.filter(d => !d.allowed)` — derived from `planned` alone — so the subtraction is exactly equal to `planned.filter(d => d.allowed).length` and cannot go negative. No change needed. Worth aligning to the filter form only if that function is edited for another reason, purely so the twins read the same.
- 2026-08-25 — **esbuild GHSA-67mh-4wv8-2f99 (≤0.24.2, dev-server CORS lets any origin read the response of any request the dev server makes)** — not applicable, and not merely "already patched": **esbuild is not in the dependency tree at all.** `grep '"esbuild"'` across all three lockfiles (root, `web/`, `mcp/`) returns nothing, and `web/node_modules/esbuild` does not exist after `npm ci --include=dev`. `web/` builds with Turbopack, not esbuild. Nothing to bump and nothing to watch — record this so a future run does not re-derive it from an advisory feed.
- 2026-08-25 — **Next.js July 2026 security release (9 CVEs, 4 high / 5 medium) — re-confirmed not applicable.** Fixed in 16.2.11 / 15.5.21, and the fixes are also carried in 16.3 stable. `web/package-lock.json` resolves `next` to **16.3.1**, so this repo is already past all nine. This restates the 2026-08-20 entry below and is recorded again only because the advisory keeps resurfacing in research feeds. **Do not confuse it with the 2026-08-26 release, which is a separate, unpatched event and is filed under Open above.**
- 2026-08-25 — **`@anthropic-ai/sdk` — investigated, and the finding corrects the premise.** The research brief described the pin as "~40 minor versions behind" against 0.72.1; `npm view @anthropic-ai/sdk version` returns **0.120.0**, so `^0.32.1` is roughly *88* minors behind, and 0.72.1 is itself stale. The pin is **not deliberate**: `git log -L` on that line in `web/package.json` shows exactly one commit, `603fb2bb feat: Agentic Engineering Knowledge Base v1.0` — it is the untouched original scaffold value, never revisited. The call sites are the most stable surface the SDK has: four `new Anthropic({ apiKey })` constructions and six `messages.create` / `messages.stream` calls taking only `model`, `max_tokens` and `messages`, consuming `content_block_delta` / `text_delta` (wire-protocol shapes defined by the API, not the SDK) and one `finalMessage()`. No `client.beta`, no `betas`, no tools, no memory tool. Notably `query/route.ts:207` already carries a comment working *around* the pin's age ("if the model returned text in a block shape this SDK version does not surface as a text_delta"). Bumped to `^0.120.0` on `nightly/2026-08-25-improvements` rather than `main`, because it is Tier B. Verified: `npx tsc --noEmit` clean, `next build` clean, `npm audit` 0 vulnerabilities. It also *reduces* supply-chain surface — the lockfile loses 21 transitive packages (the whole `node-fetch` / `form-data` / `whatwg-url` / `abort-controller` polyfill stack the modern SDK dropped for native fetch, including the deprecated `node-domexception`) and gains 4 (`standardwebhooks`, `fast-sha256`, `json-schema-to-ts`, `ts-algebra`); net −149 lockfile lines. **Residual risk, stated plainly: there is no runtime verification of the streaming path, because `web/` has no test suite (separate Open item). tsc and build are the only gates that exist.** Whoever merges should hand-drive `/api/query` once against a real key.
- 2026-08-25 — **The frontmatter codec — not touched, per the whack-a-mole rule.** No sixth same-class fix was attempted and none is proposed here. The design proposal that supersedes patching already exists as the 2026-08-22 scope correction under the Open frontmatter item ("make there be one parser", plus the corpus round-trip test as the shared gate); it did not need restating. Recorded so a future run can see the freeze was honoured deliberately rather than by omission. Two candidate defects found this run were also declined on the same rule: the `ingest-dedup.mjs` ledger-written-before-ingest-succeeds bug is the same class as `3c4cad7d` ("a run without `--route` poisoned the ledger") and is left alone, and `scripts/lib/compile-gate-core.mjs`'s `extractFrontmatter`/`extractKeyConcepts` blind spots are already enumerated under the Open frontmatter item.
- 2026-08-25 — **`lib/wiki-lint.mjs:111` — `isStalePage`'s exemption guard is dead code, and deliberately left that way for now.** `if (!isOrphanCandidate(p) && /^system\/bus\//.test(p)) return false` — `/^system\/bus\//` is item 9 in `ORPHAN_EXCLUDE_PATTERNS`, so whenever the right operand is true the left is necessarily true, and the whole expression reduces to the bus test. Either reading is a defect: if generated/append-only pages were meant to be stale-exempt (the natural reading of reusing `isOrphanCandidate`), then `daily-systems/logs/**`, `_meta/compile-log.md`, `candidates.md`, `archive/**` and every `task-log.md` are wrongly piling into the stale list; if only bus items were meant to be exempt, the `&&` clause is misleading. Not fixed because either correction moves the stale count for all ~790 pages in one commit with no way to distinguish a fixed count from a broken one — the same reasoning already applied to `buildInboundLinkMap` below. Wants a before/after corpus comparison, not a nightly patch.
- 2026-08-25 — **Two further survey findings judged real but below the bar for a nightly patch.** (a) `scripts/graph-maintenance-scan.mjs:119,145` uses `stat.ctimeMs` for "created this week"; `ctime` is inode *status-change* time, set to now by any `git clone`/`checkout`, `chmod`, rename or iCloud re-download, so after any vault-wide operation `created_this_week` approaches `notes_scanned`. `birthtimeMs` is the intended field. Cosmetic — it only feeds the "Vault Pulse" briefing line, writes nothing back. (b) `lib/repo-runtime/templates.mjs:514,532` renders `repoRecord.description` as the repository **URL** in both links on every generated repo home page, emitting `[Agentic-KB](Agent knowledge base with repo sync)` — a wrong-field read; nothing treats `description` as a URL and `syncRepo` builds real URLs from `owner`/`repo_name`. Left alone because `templates.mjs` has no test coverage at all (712 lines, zero tests, zero prior nightly commits) and changing generated-page output wants a fixture first.
- 2026-08-24 — **Claude Opus 5 (`claude-opus-5`, shipped 2026-07-24) / stale hardcoded model ids** — re-checked and still nothing to change, confirming the 2026-08-20 entry below against the newer model. `web/src/lib/model.ts` is the single knob, defaults to `claude-sonnet-5`, is overridable by `KB_MODEL`, and its own docstring already uses `claude-opus-5` as the worked example. The only other `claude-*` ids in code are the `cost-meter.mjs` pricing table (`claude-opus-4-5`, `claude-opus-4-8`, `claude-haiku-4-5`), which is a historical price list and *should* keep old ids, and it already has an `includes('opus')` family fallback so an unlisted `claude-opus-5` prices correctly. Everything else matching `claude-` across the repo is a filename, a plugin id, or vault prose. No pin to bump.
- 2026-08-24 — **Committed secrets and live-looking credentials** — full sweep over all tracked files excluding both `node_modules` trees for `sk-`, `sk-ant-`, `ghp_`, `github_pat_`, `xoxb-`, `AKIA`, `-----BEGIN … PRIVATE KEY-----` and password-bearing connection strings, plus a second pass for `*_API_KEY|SECRET|TOKEN|PASSWORD|PRIVATE_PIN` assigned a ≥16-char literal. Six hits, all confirmed non-live: `raw/framework-docs/langchain-ai-open-swe.md` is a scraped upstream setup doc whose key block is a literal `...` placeholder and whose `postgres://postgres:postgres@host.docker.internal` is the Docker default from that doc; `tests/agents/redaction.test.mjs` uses `AKIAIOSFODNN7EXAMPLE`, AWS's published documentation example, and a fake PEM as fixtures for the redactor that is supposed to catch them; `wiki/recipes/recipe-production-deployment.md` has `user:pass`. The only tracked secret-shaped files are `.env.example`, `web/.env.local.example` and `namespaces.example.json`, all templates. Nothing to redact and nothing for Jay to rotate. (Note this says nothing about the pre-commit hook's inability to *catch* such a secret — that is the separate Open item above.)
- 2026-08-24 — **MCP spec 2026-07-28 (stateless core, standardized MCP Apps + Tasks extensions, hardened OAuth 2.0/OIDC)** — no new conclusion beyond the 2026-08-20 entry below, which already covers this spec revision. `mcp/server.js` is stdio-only, so the stateless-core and OAuth halves both live in transport layers it never instantiates. Re-flagged here only so a future run does not re-derive it from the "check what spec version the server declares" prompt: the server declares none, because a stdio server has no handshake of its own.
- 2026-08-22 — **247 files under the root `node_modules/` are tracked despite being gitignored** — `.gitignore:5` lists `node_modules/`, but `git ls-files node_modules | wc -l` returns 247 (`chokidar`, `readdirp`, `yaml`). They were force-added by `6c4022f9 vault backup: 2026-04-25 09:12:34` — an automated Obsidian-git backup sweep, *not* the deliberate vendoring decision behind `mcp/node_modules`. Benign today: `npm ci` deletes and restores them byte-identically from the lockfile, so the worktree comes back clean, which is why nobody has noticed. Latent, though — the first `chokidar` or `yaml` bump will make `npm ci` leave 247 stale tracked paths modified or deleted in the worktree, which trips the dirty-worktree gate the night jobs run on (`788766e`) and buries the real diff. One `git rm -r --cached node_modules` when someone is looking; not worth doing blind at 3am because it interacts with whatever the vault-backup job is still doing.
- 2026-08-22 — **Runtime dependency sweep** — the root package has exactly two runtime deps, `chokidar@^5.0.0` and `yaml@^2.8.3`. Both are current and neither carries an advisory; `npm ci` reports `found 0 vulnerabilities` at root and in `web/`. Nothing to bump. (Note the `NODE_ENV=production` trap when checking this: the login shell on the nightly host exports it, which makes `npm ci` silently omit devDependencies and every verification tool then fails with a bogus "cannot find module". Use `npm ci --include=dev` or `unset NODE_ENV`.)
- 2026-08-22 — **`readRecentAudit` / `readRuntimeTraces` drop unparseable lines silently** (`lib/agent-runtime/audit.mjs`) — both do `lines.map(l => { try { return JSON.parse(l) } catch { return null } }).filter(Boolean)`, so a log with a torn line reads as a shorter clean log. Looks like the same fail-open shape as the registry bug fixed this run, but it is not: these are *display* readers, and the tamper-evidence path (`verifyAuditChain`) is already fail-closed — it returns `{ ok: false, reason: 'json parse', firstBreakAt: i }` on the first bad line rather than skipping it. Nothing is written back from the lenient read, so no data is destroyed. Not worth a change on its own; worth surfacing the skipped-line count if that file is edited for another reason.
- 2026-08-22 — **`buildInboundLinkMap`'s suffix fallback over-matches, suppressing orphan detection** (`lib/wiki-lint.mjs`) — when a wikilink does not resolve exactly, it falls back to `normalized.endsWith('/' + key) || key.endsWith('/' + normalized)`. A *broken* link `[[foo/bar]]` therefore matches every page whose basename is `bar`, marking it inbound-linked and hiding it from the orphan list. Real, but deliberate: the fallback exists to resolve Obsidian's shortest-unique-path links, and tightening it would move the orphan count for all 774 pages in one commit with no way to tell a fixed count from a broken one. Wants a before/after corpus comparison, not a nightly patch.
- 2026-08-22 — **`scrub-raw-pii.mjs` quarantine keeps only the first capture** — `copyToQuarantine` skips the copy if the destination exists ("the first capture wins"). If Scout re-captures a page with *new* PII and the script redacts it again, the second pristine original is destroyed in place, which contradicts the file's own Rule 1 header ("Nothing is destroyed"). Left alone: the behaviour is documented in a comment as an explicit choice, changing it means picking a versioning scheme for the quarantine, and it is a deliberate design question for Jay rather than a defect to patch at 3am.
- 2026-08-22 — **`lib/safe-ref.mjs` accepts a leading `-`, and `GET /api/agents/[id]/diff` is unauthenticated** — `SAFE_REF` permits `-` anywhere, so a ref could in principle start one and be read by `git` as an option (argument injection, not shell injection). Not reachable here: the only call site interpolates it as `` `${ref}:${file}` ``, so the argument always carries a `:config/agents/…` suffix and matches no git option, and `=` is excluded from the character class. The route being unauthenticated is real but is already covered by the `requireAuth()` chokepoint item above — adding it there rather than as a new item.
- 2026-08-21 — **Full producer→consumer lossy-boundary audit** — nine boundaries enumerated with file/function citations in `docs/LOSSY-BOUNDARIES.md`; diff against that file instead of re-deriving it. Three leaked and were fixed this run (`loadRepoContext`, `summarizeHotToLearned`, `fetchRepoMarkdown`/`syncRepo`); one is the blocked promotion item above; four were already correct (`loadAgentContext`, `mergeRewrite`, `compactHotMemory`, the bus publish paths) — `mergeRewrite` and `compactHotMemory` both snapshot the prior state before overwriting, so their loss is reversible, not merely detectable. The remaining one is **not worth doing**: `listBusItems` (`limit = 100`), `listRepoBusItems` and `searchRepoDocs` (`limit = 20`) all `.slice(0, limit)` without reporting a total, so a caller cannot tell "20 results" from "20 of 400". Low severity — the source is a directory the caller can re-scan and no artifact is written from the truncated set — and all three return bare arrays, so reporting a total is a breaking return-type change. `searchRepoDocs` also lives in `lib/repo-runtime/queries.mjs`, which is on the unmerged 08-20 branch.
- 2026-08-20 — **MCP spec 2026-07-28 (stateless core, `initialize` handshake and `Mcp-Session-Id` removed, header-based routing, cacheable list results)** — `mcp/server.js` uses `StdioServerTransport` only (line 15/1316); it never opens an HTTP listener, never reads `Mcp-Session-Id`, and implements no handshake of its own. Everything the new spec changes lives in the transport layer the SDK owns. Note for a future run: v2 does **not** ship as `@modelcontextprotocol/sdk@2` — it is two new packages, `@modelcontextprotocol/server` and `@modelcontextprotocol/client`, at `2.0.0-beta.1`, while the pinned `@modelcontextprotocol/sdk` stays on v1 (latest v1 is 1.30.0; this repo pins 1.29.0). Migrating a stdio-only server buys nothing today.
- 2026-08-20 — **Next.js July 2026 security release (App Router + Turbopack i18n middleware auth bypass, Server Actions CPU-exhaustion DoS, SSRF/open-redirect via request-controlled `rewrites()`/`redirects()`)** — `web/package-lock.json` already resolves `next` to **16.3.1**, the patched release. The repo also has no `middleware.ts` and no `rewrites`/`redirects` in `web/next.config.ts`, so two of the three do not apply structurally.
- 2026-08-20 — **Claude Opus 4.8 / stale hardcoded model ids** — no stale pin. `web/src/lib/model.ts` defaults to `claude-sonnet-5` and is overridable by `KB_MODEL`; `lib/agent-runtime/cost-meter.mjs` already has a family fallback (`includes('opus')` → opus pricing) with a comment that names `claude-opus-4-8` specifically. Nothing to change.
- 2026-08-20 — **`@anthropic-ai/sdk` path traversal in the local filesystem memory tool (GHSA-5474-4w2j-mq4c, CVSS 5.4)** — `web/` pins `0.32.1`, which predates the advisory, but the vulnerable surface is the memory tool and this repo does not use it: all four call sites (`query`, `compile`, `process`, `lint` routes) use `messages.create`/`messages.stream` only, and there is no `client.beta`, `betas`, or `memory_20…` usage anywhere in `web/src`. Not reachable. The pin is still very old and worth a deliberate bump under its own change.
- 2026-08-20 — **`kb <group> <unknown-subcommand>` exiting 0** — reported as a live bug affecting five dispatchers (a sibling of `31385a53`, which fixed it for `bus`). Not reproducible: `agent`, `repo`, `session`, `rewrite`, `canonical`, `bus` and `promote` all exit **1** on an unknown subcommand. The original reproduction read `$?` after a pipeline (`node cli/kb.js agent bogus | head -2`), which reports `head`'s status, not node's.
- 2026-08-20 — **`mcp/node_modules` committed (3,466 files)** — looks like pollution but is deliberate and long-standing: added 2026-04-07 in `c6034643` by the repo owner, not by a nightly run. A stdio MCP server is launched by Claude Desktop straight out of a clone with no install step, so vendoring is a reasonable choice here. Left alone. If it is ever un-vendored, `mcp/package-lock.json` is committed and reproduces it exactly.
- 2026-08-20 — **`/api/pending-count` "blank INGEST field marks everything ingested"** — the empty-string poisoning of the ingested set needs a log line with three or more spaces between the pipes (`/INGEST \| ([^|]+) \|/` does not match `INGEST |  |`), and `wiki/log.md` contains no such line across its 22 INGEST entries. The live half of the report is real but smaller: the `p.includes(path.basename(relPath, '.md'))` fallback marks `raw/notes/api.md` ingested because some unrelated logged path contains `api`. Left for the web test-harness item above, since there is no way to regression-test it today.
- 2026-08-20 — **Unhandled rejection from `send()` after client disconnect in the `process`/`compile` SSE routes** — `query/route.ts:315` already wraps `controller.enqueue` in try/catch with a comment explaining why, and `process`/`compile` do not. But the claimed escalation (rejection escapes `start()` and exits the process) did not reproduce: a `ReadableStream` whose `start()` rejects errors the stream rather than raising an unhandled rejection. Worth backporting the guard for the lost `error` event, not worth an unverified runtime change.
- 2026-08-21 — **`nextBusId` interpolates `channel` into a `RegExp` unescaped** (`lib/agent-runtime/ids.mjs`) — `new RegExp(\`^${channel}-${day}-(\\d{3,})\\.md$\`)`. `assertSafeSegment` rejects separators, traversal and null bytes but not regex metacharacters, so a channel named `a.b` also matches `aXb-…` filenames. Ruled out as not worth a change: the only effect is over-counting `max` within one channel's own directory, which yields a higher (still free) sequence number rather than a collision, and every channel in `config/` is a plain identifier. Worth `escapeRegExp` if that file is edited for another reason.
- 2026-08-21 — **`redact()` reusing module-level `/g` regexes across calls** (`lib/agent-runtime/redaction.mjs`) — looked like classic `lastIndex` statefulness, where a second call resumes mid-string and misses a leading match. Does not apply: `String.prototype.replace` with a global regex resets `lastIndex` to 0 before and after the scan, unlike `test`/`exec`. No shared-state bug.
- 2026-08-21 — **Frontmatter codec swap and the `requireAuth()` chokepoint (the two top Open items)** — both deliberately not started this run. `nightly/2026-08-20-improvements` is unmerged and already modifies `frontmatter.mjs` and `promotion.mjs`; rewriting either from `main` would duplicate that work or produce a conflict Jay has to resolve by hand. Gated on the new top item.
